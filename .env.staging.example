# Compatibility alias: use .env.preview.example for new deployments.
# The preview configuration is intentionally separate from local development and public production.
QELLY_ENV_TEMPLATE=.env.preview.example
