export async function renderPlatformReadiness(main,{api,pageHead,escapeHtml}){
 const data=await api('/api/v1/platform/readiness');
 const tone=status=>status==='ready'?'live':status==='deferred'?'cached':status==='partial'?'warning':'unavailable';
 main.innerHTML=`<section class="q-page">${pageHead('Production readiness','Platform Readiness','One evidence surface for database, workers, storage quarantine, secret rotation, outbound policy, recovery, passkeys, assurance drills, staging, and financial-safety gates.')}<div class="q-kpi-grid"><article class="q-kpi"><div class="q-kpi-label">Ready gates</div><div class="q-kpi-value">${data.summary.ready}</div><div class="q-kpi-meta"><span>Runnable locally</span><span class="q-status q-status--live">verified</span></div></article><article class="q-kpi"><div class="q-kpi-label">Partial</div><div class="q-kpi-value">${data.summary.partial}</div><div class="q-kpi-meta"><span>Adapter or configuration</span><span class="q-status q-status--warning">review</span></div></article><article class="q-kpi"><div class="q-kpi-label">Deferred</div><div class="q-kpi-value">${data.summary.deferred}</div><div class="q-kpi-meta"><span>External infrastructure</span><span class="q-status q-status--cached">planned</span></div></article><article class="q-kpi"><div class="q-kpi-label">Blocked</div><div class="q-kpi-value">${data.summary.blocked}</div><div class="q-kpi-meta"><span>Not release-ready</span><span class="q-status q-status--unavailable">blocked</span></div></article></div><section class="q-panel"><div class="q-panel-head"><div><h2>Dependency gates</h2><p>Statuses are derived from the running application, not from design-atlas entries.</p></div></div><div class="q-panel-body q-stack">${data.gates.map(g=>`<div class="q-record-row"><span><strong>${escapeHtml(g.label)}</strong><small>${escapeHtml(g.detail)}</small></span><span class="q-status q-status--${tone(g.status)}">${escapeHtml(g.status)}</span></div>`).join('')}</div></section><section class="q-panel"><div class="q-panel-head"><div><h2>Safety boundary</h2><p>Execution and custody remain intentionally absent.</p></div></div><div class="q-panel-body"><ul>${Object.entries(data.safety).map(([key,value])=>`<li><strong>${escapeHtml(key)}</strong>: ${value?'enabled':'disabled'}</li>`).join('')}</ul></div></section></section>`;
}
