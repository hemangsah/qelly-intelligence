# Charting

A chart shell and adapter TypeScript contract with pan/zoom controls, source metadata and a table alternative. The bundled renderer is deterministic and local; it is not a licensed production chart feed.
