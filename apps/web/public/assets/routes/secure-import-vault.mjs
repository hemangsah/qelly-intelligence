export async function renderSecureImportVault(main,{api,pageHead,toast,escapeHtml}){
 const data=await api('/api/v1/secure-imports');
 main.innerHTML=`<section class="q-page">${pageHead('Secure ingestion','Secure Import Vault','Stage files through bounded object storage, hashing, malware-signature checks and tenant-scoped metadata.')}<div class="q-two-column"><section class="q-panel"><div class="q-panel-head"><div><h2>Upload to staging</h2><p>10 MB maximum; local object-store adapter in development.</p></div></div><form class="q-panel-body q-auth-form" data-form><label>File<input type="file" name="file" required></label><button class="q-button q-button--primary">Scan and store</button></form></section><section class="q-panel"><div class="q-panel-head"><div><h2>Stored objects</h2><p>Content-addressed and tenant scoped.</p></div><span class="q-status q-status--cached">${data.items.length}</span></div><div class="q-panel-body q-stack">${data.items.map(x=>`<div class="q-record-row"><span><strong>${escapeHtml(x.file_name)}</strong><small>${x.size} bytes · ${x.sha256.slice(0,16)}…</small></span><span class="q-status q-status--live">${x.status}</span></div>`).join('')||'<p>No secure imports yet.</p>'}</div></section></div></section>`;
 main.querySelector('[data-form]').onsubmit=async ev=>{ev.preventDefault();const f=new FormData(ev.currentTarget).get('file');if(!f)return;const contentBase64=await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result).split(',')[1]);r.onerror=reject;r.readAsDataURL(f)});try{await api('/api/v1/secure-imports',{method:'POST',body:JSON.stringify({fileName:f.name,mimeType:f.type||'application/octet-stream',contentBase64})});toast('File scanned and stored',{tone:'success'});location.hash='secure-import-vault';location.reload();}catch(e){toast(e.message,{tone:'danger'});}};
}
