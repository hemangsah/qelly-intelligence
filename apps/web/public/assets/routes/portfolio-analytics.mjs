function evidenceInspector({overview,holdings,performance,risk},escapeHtml){
 const rows=[
  ['TRUTH STATE','Deterministic model portfolio · not live'],
  ['SOURCE','Qelly deterministic portfolio performance fixture'],
  ['OBSERVED AT',overview.asOf??'Not supplied'],
  ['CONFIDENCE','0.82 chart evidence metadata'],
  ['COVERAGE',`${holdings.items.length} holdings · ${performance.points.length} performance observations`],
  ['METHOD',risk.methodology??'Not supplied'],
  ['CUSTODY','No custodian or broker connection']
 ];
 return `<aside class="q-v53-evidence-inspector" aria-label="Intelligence Inspector"><header><strong>INTELLIGENCE INSPECTOR</strong><span>Portfolio evidence is separated from connected-account or execution state.</span></header>${rows.map(([label,value])=>`<div class="q-v53-evidence-row"><small>${label}</small><strong>${escapeHtml(String(value))}</strong></div>`).join('')}<div class="q-v53-evidence-note">Research model only. Holdings are not reconciled brokerage balances, cash is not custodial cash, and no order, transfer or wallet action exists.</div></aside>`;
}

export async function renderPortfolioAnalytics(main,deps){
 const {api,pageHead,stateBanner,escapeHtml,QellyDataGrid,QellyChartShell,formatCompact}=deps;
 const [overview,holdings,performance,risk]=await Promise.all([api('/api/v1/portfolio/overview'),api('/api/v1/portfolio/holdings'),api('/api/v1/portfolio/performance?range=1y'),api('/api/v1/portfolio/risk')]);
 main.innerHTML=`<section class="q-page">${pageHead('Qelly Intelligence','Portfolio Analytics','Deterministic model-portfolio holdings, performance, concentration and stress analytics. No connected account, broker reconciliation or order execution exists.',`<button class="q-button q-button--secondary" data-action="research">Open research workspace</button><button class="q-button q-button--secondary" disabled aria-disabled="true">Broker connections disabled</button>`)}${stateBanner()}
 <div class="q-kpi-grid"><article class="q-kpi"><div class="q-kpi-label">Portfolio value</div><div class="q-kpi-value">${formatCompact(overview.totalValue)}</div><div class="q-kpi-meta"><span>${overview.baseCurrency}</span><span class="q-status q-status--simulated">fixture</span></div></article><article class="q-kpi"><div class="q-kpi-label">Unrealized P&amp;L</div><div class="q-kpi-value">${overview.unrealizedPnl>=0?'+':''}${formatCompact(overview.unrealizedPnl)}</div><div class="q-kpi-meta"><span>${overview.unrealizedPnlPct}%</span><span class="q-status q-status--cached">calculated</span></div></article><article class="q-kpi"><div class="q-kpi-label">Day P&amp;L</div><div class="q-kpi-value">${overview.dayPnl>=0?'+':''}${formatCompact(overview.dayPnl)}</div><div class="q-kpi-meta"><span>${overview.dayPnlPct}%</span><span class="q-status q-status--simulated">snapshot</span></div></article><article class="q-kpi"><div class="q-kpi-label">Cash</div><div class="q-kpi-value">${formatCompact(overview.cashValue)}</div><div class="q-kpi-meta"><span>unconnected</span><span class="q-status q-status--unavailable">no custodian</span></div></article></div>
 <div class="q-v53-evidence-workspace"><div class="q-v53-evidence-primary">
  <div class="q-two-column"><div id="portfolio-chart"></div><section class="q-panel"><div class="q-panel-head"><div><h2>Risk snapshot</h2><p>${escapeHtml(risk.methodology)}</p></div><span class="q-status q-status--simulated">not live</span></div><div class="q-panel-body q-stack"><div class="q-record-row"><span><strong>Annualized volatility</strong><small>deterministic scenario metric</small></span><strong>${risk.riskMetrics.annualizedVolatilityPct}%</strong></div><div class="q-record-row"><span><strong>95% one-day VaR</strong><small>local analytical estimate</small></span><strong>${risk.riskMetrics.valueAtRisk95OneDayPct}%</strong></div><div class="q-record-row"><span><strong>Maximum drawdown</strong><small>packaged performance history</small></span><strong>${risk.riskMetrics.maxDrawdownPct}%</strong></div><div class="q-record-row"><span><strong>Largest position</strong><small>${escapeHtml(risk.concentration.largestPosition.symbol)}</small></span><strong>${risk.concentration.largestPosition.weightPct}%</strong></div></div></section></div>
  <section class="q-panel"><div class="q-panel-head"><div><h2>Holdings</h2><p>Research model positions, not reconciled brokerage balances.</p></div><span class="q-status q-status--unavailable">non-tradable</span></div><div id="portfolio-grid"></div></section>
  <section class="q-panel"><div class="q-panel-head"><div><h2>Stress scenarios</h2><p>Static local scenarios for UI and API contract validation.</p></div><span class="q-status q-status--simulated">scenario only</span></div><div class="q-panel-body q-stack">${risk.stressScenarios.map(item=>`<div class="q-record-row"><span><strong>${escapeHtml(item.name)}</strong><small>estimated portfolio impact</small></span><strong class="is-negative">${item.estimatedImpactPct}%</strong></div>`).join('')}</div></section>
 </div>${evidenceInspector({overview,holdings,performance,risk},escapeHtml)}</div>
 <section class="q-v53-evidence-activity" aria-label="Portfolio evidence activity"><span>READ ONLY</span><span>${escapeHtml(String(overview.asOf??'as-of unavailable'))}</span><span>${holdings.items.length} model holdings</span><span>${performance.points.length} performance observations</span><span>${risk.stressScenarios.length} stress scenarios</span><span>Broker and custody unavailable</span></section></section>`;
 new QellyChartShell(document.getElementById('portfolio-chart'),{title:'Portfolio versus blended benchmark',series:performance.points.map(item=>({label:item.at,value:item.portfolio})),metadata:{source:'Qelly deterministic portfolio performance fixture',observedAt:overview.asOf,receivedAt:overview.asOf,freshnessClass:'simulated',confidence:0.82}});
 new QellyDataGrid(document.getElementById('portfolio-grid'),{caption:'Model portfolio holdings',columns:[{key:'symbol',label:'Symbol',width:90},{key:'name',label:'Holding',width:190},{key:'assetClass',label:'Class',width:100},{key:'quantity',label:'Quantity',width:100,numeric:true},{key:'price',label:'Price',width:110,numeric:true},{key:'marketValue',label:'Market value',width:130,numeric:true},{key:'unrealizedPnl',label:'Unrealized P&L',width:135,numeric:true},{key:'unrealizedPnlPct',label:'P&L %',width:100,numeric:true},{key:'weightPct',label:'Weight',width:100,numeric:true},{key:'freshnessClass',label:'Freshness',width:110,format:'status'}],rows:holdings.items});
 main.querySelector('[data-action="research"]').addEventListener('click',()=>deps.navigate('research-workspace'));
}
