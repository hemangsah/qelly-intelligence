const STYLE_ID='qelly-event-calendar-v2-style';
function ensureStyles(){if(document.getElementById(STYLE_ID))return;const link=document.createElement('link');link.id=STYLE_ID;link.rel='stylesheet';link.href='./assets/routes/event-calendar-v2.css?v=20260901-event2';document.head.append(link);}
const status=(value)=>String(value||'blocked').toUpperCase().replaceAll('-',' ');

export async function renderEventCalendar(main,deps){
  const {api,escapeHtml,navigate,asset,toast}=deps;ensureStyles();
  const safe=(value)=>escapeHtml(String(value??''));
  const endpoint=(selected,params='')=>`/api/v1/discovery/event-calendar?asset=${encodeURIComponent(selected||'QI-EQUITY-AAPL')}${params}`;
  let data;
  try{data=await api(endpoint(asset));}catch(error){main.innerHTML=`<section class="q-ec-page"><div class="q-ec-error"><p class="q-eyebrow">Event planning unavailable</p><h1>The monitoring contract could not be loaded.</h1><p>${safe(error.message)}</p><button data-ec-retry>Retry</button></div></section>`;main.querySelector('[data-ec-retry]')?.addEventListener('click',()=>renderEventCalendar(main,deps));return;}

  const paint=(model)=>{
    data=model;const {selected,plan,readiness,coverage,eventGuide,monitoringWindow}=model;
    const sourceGuide=model.approvedSourceDomains.map((domain)=>`<span>${safe(domain)}</span>`).join('');
    main.innerHTML=`<section class="q-ec-page" data-event-experience="governed-event-calendar-v2" data-plan-state="${safe(plan.state)}">
      <section class="q-ec-hero"><div><p class="q-eyebrow">Qelly Intelligence · Event Calendar</p><h1>Turn uncertainty into an event plan.</h1><p>${safe(model.job)}</p><div class="q-ec-hero-actions"><label><span>Asset</span><select aria-label="Select event asset" data-ec-asset>${model.assets.map((item)=>`<option value="${safe(item.id)}" ${item.id===selected.id?'selected':''}>${safe(item.symbol)} · ${safe(item.name)}</option>`).join('')}</select></label><button data-ec-provenance>Open Decision Provenance</button></div></div><aside><span>Unique job · catalyst choreography</span><strong>One event. Two outcomes. One accountable review.</strong><p>Filings prove issuer language. Alerts deliver triggers. This workspace defines what must be checked before, during and after a future catalyst.</p></aside></section>
      <section class="q-ec-boundary"><span></span><div><strong>No connected calendar feed · event planning remains operational</strong><p>${safe(coverage.reason)} Qelly does not fetch the source, verify its contents, create an alert or invent a date.</p></div><a href="https://www.sec.gov/edgar/search/" target="_blank" rel="noopener noreferrer nofollow">Find a primary source ↗</a></section>
      <section class="q-ec-snapshot" aria-label="Event planning summary"><article><span>Resolved asset</span><strong>${safe(selected.symbol)}</strong><small>${safe(selected.assetClass)}</small></article><article><span>Connected events</span><strong>${coverage.connectedEvents}</strong><small>No event substituted</small></article><article><span>Monitoring plans</span><strong>${coverage.registeredPlans}</strong><small>${safe(plan.state)}</small></article><article><span>Evidence gates</span><strong>${readiness.readyGates} / ${readiness.totalGates}</strong><small>${safe(readiness.state)}</small></article></section>
      <section class="q-ec-workbench"><header><div><p class="q-eyebrow">Step 01 · declare the catalyst</p><h2>Build the monitoring receipt before the outcome.</h2><p>The backend validates timing, source authority, thesis exposure, outcome criteria and ownership. Nothing is persisted or scheduled.</p></div><button type="button" data-ec-reset>Clear plan</button></header><div class="q-ec-workbench-grid">
        <form data-ec-form>
          <label><span>Event type</span><select name="eventType" aria-label="Event type">${model.eventTypes.map((item)=>`<option value="${safe(item.id)}" ${item.id===plan.eventType?'selected':''}>${safe(item.label)}</option>`).join('')}</select></label>
          <label><span>Date confidence</span><select name="dateConfidence" aria-label="Date confidence">${model.dateConfidence.map((item)=>`<option value="${safe(item.id)}" ${item.id===plan.dateConfidence?'selected':''}>${safe(item.label)}</option>`).join('')}</select></label>
          <label class="q-ec-wide"><span>Event title</span><input name="title" aria-label="Event title" maxlength="140" value="${safe(plan.title)}" placeholder="Name one precise catalyst"></label>
          <label><span>Scheduled date</span><input type="date" name="scheduledDate" aria-label="Scheduled date" value="${safe(plan.scheduledDate)}"></label>
          <label><span>Scheduled time</span><input type="time" name="scheduledTime" aria-label="Scheduled time" value="${safe(plan.scheduledTime)}"></label>
          <label><span>Timezone</span><select name="timezone" aria-label="Event timezone">${model.timezones.map((item)=>`<option value="${safe(item)}" ${item===plan.timezone?'selected':''}>${safe(item)}</option>`).join('')}</select></label>
          <label><span>Review window</span><select name="windowDays" aria-label="Review window days">${[1,2,3,5,7,14,30].map((item)=>`<option value="${item}" ${item===plan.windowDays?'selected':''}>± ${item} day${item===1?'':'s'}</option>`).join('')}</select></label>
          <label class="q-ec-wide"><span>Primary source URL</span><input type="url" name="sourceUrl" aria-label="Primary source URL" value="${safe(plan.sourceUrl)}" placeholder="https://www.sec.gov/…"><small>Approved issuer, regulator, exchange or protocol authority</small></label>
          <label class="q-ec-wide"><span>Thesis exposure</span><textarea name="thesisExposure" aria-label="Thesis exposure" rows="3" maxlength="420" placeholder="Which thesis variable could this event change?">${safe(plan.thesisExposure)}</textarea></label>
          <label class="q-ec-wide"><span>Pre-event question</span><textarea name="preEventQuestion" aria-label="Pre-event question" rows="3" maxlength="420" placeholder="What exact question must the official outcome answer?">${safe(plan.preEventQuestion)}</textarea></label>
          <label><span>Confirming signal</span><textarea name="confirmationSignal" aria-label="Confirming signal" rows="4" maxlength="420" placeholder="Evidence that strengthens the thesis">${safe(plan.confirmationSignal)}</textarea></label>
          <label><span>Adverse signal</span><textarea name="adverseSignal" aria-label="Adverse signal" rows="4" maxlength="420" placeholder="Evidence that weakens or invalidates it">${safe(plan.adverseSignal)}</textarea></label>
          <label class="q-ec-wide"><span>Review owner</span><input name="owner" aria-label="Review owner" maxlength="100" value="${safe(plan.owner)}" placeholder="Person or role accountable for follow-up"></label>
          <button class="q-ec-run" type="submit">Validate and build monitoring plan</button>
        </form>
        <aside class="q-ec-receipt"><header><div><span>Monitoring receipt</span><h3>${safe(selected.symbol)} · ${safe(model.eventTypes.find((item)=>item.id===plan.eventType)?.label)}</h3></div><b>${status(plan.state)}</b></header><dl><div><dt>Plan ID</dt><dd>${safe(plan.planId||'Created after all eight gates pass')}</dd></div><div><dt>Integrity fingerprint</dt><dd>${safe(plan.fingerprint)}</dd></div><div><dt>Date confidence</dt><dd>${safe(model.dateConfidence.find((item)=>item.id===plan.dateConfidence)?.label)}</dd></div><div><dt>Schedule</dt><dd>${safe(plan.scheduledDate||'Missing')} · ${safe(plan.scheduledTime||'Missing')} · ${safe(plan.timezone)}</dd></div><div><dt>Monitoring window</dt><dd>${safe(monitoringWindow.start||'Missing')} → ${safe(monitoringWindow.end||'Missing')}</dd></div><div><dt>Source authority</dt><dd>${safe(plan.sourceAuthority||'Missing')}</dd></div><div><dt>Owner</dt><dd>${safe(plan.owner||'Missing')}</dd></div><div><dt>Notification</dt><dd>Not scheduled</dd></div></dl><section><strong>${plan.state==='ready'?'Ready for human monitoring review':'Monitoring plan correctly remains a draft'}</strong><p>${plan.state==='ready'?'Reopen the primary source before the event and compare the official result with both predeclared outcomes.':'Complete the blocked gates. Qelly will not infer a date, source, outcome or owner.'}</p></section><div><button type="button" data-ec-copy>Copy monitoring brief</button><button type="button" data-ec-send>Send to provenance →</button></div></aside>
      </div></section>
      <section class="q-ec-gates"><header><div><p class="q-eyebrow">Step 02 · qualify the plan</p><h2>Eight gates before one catalyst enters a decision.</h2></div><span>${readiness.readyGates} READY · ${readiness.totalGates-readiness.readyGates} BLOCKED</span></header><div>${model.gates.map((gate,index)=>`<article data-gate-state="${safe(gate.state)}"><header><span>${String(index+1).padStart(2,'0')}</span><b>${status(gate.state)}</b></header><h3>${safe(gate.label)}</h3><p>${safe(gate.purpose)}</p><small>${safe(gate.detail)}</small></article>`).join('')}</div></section>
      <section class="q-ec-guide"><div><p class="q-eyebrow">Step 03 · frame the event</p><h2 data-ec-guide-title>${safe(eventGuide.label)}</h2><p data-ec-guide-purpose>${safe(eventGuide.purpose)}</p><ol data-ec-guide-questions>${eventGuide.questions.map((question,index)=>`<li><span>${String(index+1).padStart(2,'0')}</span>${safe(question)}</li>`).join('')}</ol></div><aside><span>Approved source authorities</span><p>Domain approval qualifies the authority—not the truth of the page contents. The reviewer must still open and verify the source.</p><div>${sourceGuide}</div></aside></section>
      <section class="q-ec-outcomes"><header><div><p class="q-eyebrow">Step 04 · precommit the interpretation</p><h2>Record both sides before the result is known.</h2><p>This prevents a surprising event from being explained away after it happens.</p></div></header><div><article data-outcome="confirm"><span>CONFIRMING</span><h3>What would strengthen the thesis?</h3><p>${safe(plan.confirmationSignal||'Declare the evidence that would count as confirming.')}</p></article><article data-outcome="adverse"><span>ADVERSE</span><h3>What would weaken or invalidate it?</h3><p>${safe(plan.adverseSignal||'Declare the evidence that would count as adverse.')}</p></article><article data-outcome="unresolved"><span>UNRESOLVED</span><h3>What if the official outcome answers neither?</h3><p>Preserve uncertainty, collect counter-evidence and schedule a new review. Do not force the result into either thesis bucket.</p></article></div></section>
      <section class="q-ec-timeline"><header><p class="q-eyebrow">Monitoring protocol</p><h2>Five review moments around the event.</h2></header><div>${model.reviewTimeline.map((item)=>`<article><span>${safe(item.when)}</span><h3>${safe(item.phase)}</h3><p>${safe(item.job)}</p></article>`).join('')}</div></section>
      <section class="q-ec-handoffs"><header><div><p class="q-eyebrow">Specialist handoffs</p><h2>Carry the event without duplicating another feature.</h2><p>Sources move to Filing Workspace, context to Qelly Chat, the market baseline to Chart Studio, and the monitoring receipt to Decision Provenance.</p></div></header><div>${model.handoffs.map((item)=>`<button type="button" data-ec-route="${safe(item.route)}"><strong>${safe(item.label)}</strong><i>Open →</i><span>${safe(item.job)}</span></button>`).join('')}</div></section>
      <footer class="q-ec-footer"><span>User-declared planning contract · no connected event feed · no source content fetched</span><span>No persistence · no alerts · no recommendation · no execution</span></footer>
    </section>`;
    bind();
  };

  const bind=()=>{
    const form=main.querySelector('[data-ec-form]');
    main.querySelector('[data-ec-asset]').addEventListener('change',(event)=>navigate('event-calendar',event.target.value));
    main.querySelector('[data-ec-provenance]').addEventListener('click',()=>navigate('decision-provenance'));
    main.querySelector('[data-ec-send]').addEventListener('click',()=>navigate('decision-provenance'));
    main.querySelectorAll('[data-ec-route]').forEach((button)=>button.addEventListener('click',()=>navigate(button.dataset.ecRoute,data.selected.id)));
    main.querySelector('[data-ec-reset]').addEventListener('click',()=>{form.reset();toast?.('Event plan fields cleared.');});
    main.querySelector('[data-ec-copy]').addEventListener('click',async()=>{const text=[`${data.selected.symbol} · ${data.plan.title||'Event plan'}`,`Plan: ${data.plan.planId||'draft'}`,`Fingerprint: ${data.plan.fingerprint}`,`Schedule: ${data.plan.scheduledDate||'Missing'} ${data.plan.scheduledTime||''} ${data.plan.timezone}`,`Question: ${data.plan.preEventQuestion||'Missing'}`,`Confirming: ${data.plan.confirmationSignal||'Missing'}`,`Adverse: ${data.plan.adverseSignal||'Missing'}`,`Source: ${data.plan.sourceUrl||'Missing'}`].join('\n');try{await navigator.clipboard.writeText(text);toast?.('Monitoring brief copied.');}catch{toast?.('Clipboard access is unavailable.');}});
    const typeMap=new Map(data.eventTypes.map((item)=>[item.id,item]));
    form.elements.eventType.addEventListener('change',()=>{const item=typeMap.get(form.elements.eventType.value);main.querySelector('[data-ec-guide-title]').textContent=item.label;main.querySelector('[data-ec-guide-purpose]').textContent=item.purpose;main.querySelector('[data-ec-guide-questions]').innerHTML=item.questions.map((question,index)=>`<li><span>${String(index+1).padStart(2,'0')}</span>${safe(question)}</li>`).join('');});
    form.addEventListener('submit',async(event)=>{event.preventDefault();const submit=form.querySelector('[type="submit"]');submit.disabled=true;submit.textContent='Validating…';const values=new URLSearchParams(new FormData(form));try{const next=await api(endpoint(data.selected.id,`&${values.toString()}`));paint(next);toast?.(next.plan.state==='ready'?'Monitoring receipt ready for human review.':'Event plan updated; blocked gates remain.');}catch(error){submit.disabled=false;submit.textContent='Validate and build monitoring plan';toast?.(`Event plan unavailable: ${error.message}`);}});
  };
  paint(data);
}
