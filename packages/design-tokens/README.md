# Design Tokens

Semantic theme profiles, protected financial-state aliases, density, motion, typography and the theme profile schema. Burgundy Command is the default. Consumer overrides must not remap protected semantics.
