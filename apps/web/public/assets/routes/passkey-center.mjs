import {supportsPasskeys,registrationOptions,serializeRegistration} from '../webauthn.mjs';
export async function renderPasskeyCenter(main,{api,pageHead,toast,escapeHtml,renderRoute}){
 const data=await api('/api/v1/auth/passkeys');const supported=supportsPasskeys();
 main.innerHTML=`<section class="q-page">${pageHead('Passwordless assurance','Passkey Center','Register platform or roaming WebAuthn credentials. The server verifies RP ID, origin, challenge, user presence, ES256 public key and authentication signatures.')}<div class="q-kpi-grid"><article class="q-kpi"><div class="q-kpi-label">Registered passkeys</div><div class="q-kpi-value">${data.items.length}</div><div class="q-kpi-meta"><span>${escapeHtml(data.rpId)}</span><span class="q-status q-status--${supported?'live':'warning'}">${supported?'browser ready':'unsupported'}</span></div></article><article class="q-kpi"><div class="q-kpi-label">Attestation</div><div class="q-kpi-value q-kpi-value--text">None</div><div class="q-kpi-meta"><span>Privacy preserving</span><span class="q-status q-status--cached">ES256</span></div></article><article class="q-kpi"><div class="q-kpi-label">Login assurance</div><div class="q-kpi-value q-kpi-value--text">High</div><div class="q-kpi-meta"><span>Signed challenge</span><span class="q-status q-status--live">WebAuthn</span></div></article></div><div class="q-two-column"><section class="q-panel"><div class="q-panel-head"><div><h2>Your passkeys</h2><p>Credentials are scoped to this account. Qelly stores public keys only.</p></div><button class="q-button q-button--primary" data-register ${supported?'':'disabled'}>Register passkey</button></div><div class="q-panel-body q-stack">${data.items.length?data.items.map(item=>`<div class="q-record-row"><span><strong>${escapeHtml(item.label)}</strong><small>${escapeHtml(item.credentialId.slice(0,28))}… · ${item.lastUsedAt?`last used ${new Date(item.lastUsedAt).toLocaleString()}`:'not used yet'}</small></span><button class="q-button q-button--ghost" data-revoke="${escapeHtml(item.credentialId)}">Revoke</button></div>`).join(''):'<div class="q-empty-state"><h3>No passkeys registered</h3><p>Register a device passkey, security key, or synced platform credential.</p></div>'}</div></section><section class="q-panel"><div class="q-panel-head"><div><h2>Verification boundary</h2><p>Local implementation, production-compatible ceremony.</p></div><span class="q-status q-status--warning">origin bound</span></div><div class="q-panel-body"><ul><li>Challenge expires after five minutes and is single-use.</li><li>RP ID and origin are validated server-side.</li><li>Only ES256/P-256 credentials are accepted by the current implementation.</li><li>Credential signature counters are checked when supported.</li><li>Private keys remain inside the authenticator.</li></ul></div></section></div></section>`;
 main.querySelector('[data-register]')?.addEventListener('click',async()=>{try{const label=prompt('Name this passkey','Primary device')||'Qelly passkey';const options=await api('/api/v1/auth/passkeys/register/options',{method:'POST',body:JSON.stringify({label})});const credential=await navigator.credentials.create({publicKey:registrationOptions(options.publicKey)});if(!credential)throw new Error('Passkey registration was cancelled');await api('/api/v1/auth/passkeys/register/verify',{method:'POST',body:JSON.stringify({challengeId:options.challengeId,credential:serializeRegistration(credential)})});toast('Passkey registered',{tone:'success'});await renderRoute();}catch(error){toast(error.message,{tone:'danger'});}});
 main.querySelectorAll('[data-revoke]').forEach(button=>button.addEventListener('click',async()=>{if(!confirm('Revoke this passkey?'))return;try{await api(`/api/v1/auth/passkeys/${encodeURIComponent(button.dataset.revoke)}`,{method:'DELETE',body:'{}'});toast('Passkey revoked',{tone:'success'});await renderRoute();}catch(error){toast(error.message,{tone:'danger'});}}));
}
