const tone=(value)=>{const state=String(value||'').toUpperCase();if(['ENABLED','REFERENCE_ENABLED','MATCH','PASS'].includes(state))return 'live';if(['DELAYED','WARNING'].includes(state))return 'delayed';if(['UNAVAILABLE','DENY','MISMATCH'].includes(state))return 'unavailable';return 'cached';};
const date=(value)=>{const parsed=new Date(value||'');return Number.isNaN(parsed.getTime())?'Not supplied':parsed.toLocaleString('en-IN');};

export async function renderProviderRuntimeV6(main,deps){
  const {api,pageHead,stateBanner,escapeHtml}=deps;
  let providerData,incidents,contract,plane;
  try{[providerData,incidents,contract,plane]=await Promise.all([api('/api/v1/providers/runtime'),api('/api/v1/data-quality/incidents'),api('/api/v1/contracts/entitlements'),api('/api/v1/platform/data-plane?limit=12')]);}
  catch(error){main.innerHTML=`<section class="q-page">${pageHead('Qelly Intelligence · Runtime controls','Provider Runtime','Canonical provider runtime evidence is unavailable. Qelly will not replace it with local fixture state.','')}${stateBanner()}<section class="q-panel"><div class="q-panel-body"><div class="q-empty-state"><strong>Provider runtime unavailable</strong><p>${escapeHtml(error.message)}</p></div></div></section></section>`;return;}
  const providers=Array.isArray(providerData.providers)?providerData.providers:[];
  const openIncidents=Array.isArray(incidents.items)?incidents.items.filter((item)=>item.status!=='resolved'):[];
  const congruence=plane.releaseCongruence||{state:'UNVERIFIED'};
  const cells=[
    ['Instruments',plane.dataPlane?.instrumentCount||0,'governed master'],
    ['Series',plane.dataPlane?.seriesCount||0,'normalized provider series'],
    ['Observations',plane.dataPlane?.pointCount||0,'persisted points'],
    ['Providers',providers.length,'production policy registry'],
    ['Quality incidents',openIncidents.length,'open governed events'],
    ['Release',congruence.state,'Cloudflare ↔ persisted identity']
  ];
  const providerRows=providers.map((provider)=>`<article class="q-v6-runtime-provider"><div><span class="q-v6-runtime-dot q-v6-runtime-dot--${tone(provider.runtimeState)}"></span><div><strong>${escapeHtml(provider.id.toUpperCase())}</strong><small>${escapeHtml((provider.capabilities||[]).join(' · ')||'No active capabilities')}</small></div></div><span class="q-status q-status--${tone(provider.runtimeState)}">${escapeHtml(provider.runtimeState)}</span><dl><dt>Policy</dt><dd>${provider.policyEnabled?'enabled':'blocked'}</dd><dt>Terms</dt><dd>${escapeHtml(provider.termsState||'not supplied')}</dd><dt>Reason</dt><dd>${escapeHtml(provider.reason||'No blocking reason')}</dd></dl>${provider.termsUrl?`<a href="${escapeHtml(provider.termsUrl)}" target="_blank" rel="noopener noreferrer nofollow">Provider terms ↗</a>`:''}</article>`).join('');
  const qualityRows=openIncidents.map((item)=>`<div class="q-v6-runtime-row"><span><strong>${escapeHtml(item.eventType||item.type||'quality event')}</strong><small>${escapeHtml(date(item.detectedAt))}</small></span><span class="q-status q-status--${tone(item.severity)}">${escapeHtml(item.severity||'review')}</span></div>`).join('')||'<div class="q-empty-state"><strong>No open provider-quality incidents</strong><p>The governed quality event store is currently clear.</p></div>';
  const latest=Array.isArray(plane.items)?plane.items.slice(0,8):[];
  const latestRows=latest.map((item)=>`<div class="q-v6-runtime-row"><span><strong>${escapeHtml(item.symbol)}</strong><small>${escapeHtml(item.displayName||'')} · ${escapeHtml(item.unit||'')}</small></span><span><strong>${escapeHtml(String(item.value??'—'))}</strong><small>${escapeHtml(date(item.observedAt))}</small></span><span class="q-status q-status--${tone(item.truthState)}">${escapeHtml(item.truthState||'UNAVAILABLE')}</span></div>`).join('');
  main.innerHTML=`<section class="q-page q-v6-runtime-page">
    ${pageHead('Qelly Intelligence · Canonical provider controls','Provider Runtime & Data Quality','Production provider policy, normalized observations, release identity and entitlement evidence. Blocked providers remain blocked; no deterministic fixture is presented as a live provider.',`<a class="q-button q-button--secondary" href="#/live-markets">Market Command</a><a class="q-button q-button--primary" href="#/timeseries-lab">Reference history</a>`)}${stateBanner()}
    <section class="q-v6-runtime-kpis">${cells.map(([label,value,meta])=>`<div><span>${escapeHtml(label)}</span><strong>${escapeHtml(String(value))}</strong><small>${escapeHtml(meta)}</small></div>`).join('')}</section>
    <section class="q-v6-release-strip"><div><span>Canonical runtime</span><strong>${escapeHtml(plane.canonicalRuntime||'cloudflare-pages-functions')}</strong></div><div><span>Runtime SHA</span><strong>${escapeHtml(String(plane.releaseSha||'').slice(0,12)||'unresolved')}</strong></div><div><span>Recorded SHA</span><strong>${escapeHtml(String(congruence.recordedSha||'').slice(0,12)||'unverified')}</strong></div><div><span>Congruence</span><span class="q-status q-status--${tone(congruence.state)}">${escapeHtml(congruence.state)}</span></div><div><span>Execution</span><strong>DISABLED</strong></div></section>
    <div class="q-v6-runtime-layout">
      <section class="q-panel"><div class="q-panel-head"><div><h2>Provider registry</h2><p>Rights and runtime state determine whether a provider can be used.</p></div><span class="q-status q-status--cached">${providers.length} providers</span></div><div class="q-panel-body q-v6-runtime-provider-grid">${providerRows}</div></section>
      <aside class="q-panel"><div class="q-panel-head"><div><h2>Entitlement boundary</h2><p>Server-side deny-by-default policy.</p></div><span class="q-status q-status--unavailable">${escapeHtml(contract.decision)}</span></div><div class="q-panel-body"><dl class="q-v6-evidence-list"><dt>Evaluation</dt><dd>${escapeHtml(contract.evaluation)}</dd><dt>Dimensions</dt><dd>${escapeHtml((contract.dimensions||[]).join(' · '))}</dd><dt>Obligations</dt><dd>${escapeHtml((contract.obligations||[]).join(' · '))}</dd><dt>Execution</dt><dd>DENY</dd><dt>Custody</dt><dd>DENY</dd><dt>Money movement</dt><dd>DENY</dd></dl></div></aside>
    </div>
    <div class="q-v6-runtime-lower">
      <section class="q-panel"><div class="q-panel-head"><div><h2>Latest governed observations</h2><p>First-party normalized data; values retain provider truth state.</p></div><a class="q-button q-button--ghost" href="#/timeseries-lab">Open history</a></div><div class="q-panel-body">${latestRows||'<div class="q-empty-state">No governed observations.</div>'}</div></section>
      <section class="q-panel"><div class="q-panel-head"><div><h2>Data quality</h2><p>Persistent ingestion/schema/freshness incidents.</p></div><span class="q-status q-status--${openIncidents.length?'delayed':'live'}">${openIncidents.length?`${openIncidents.length} open`:'clear'}</span></div><div class="q-panel-body">${qualityRows}</div></section>
    </div>
    <section class="q-panel q-v6-runtime-boundary"><div class="q-panel-body"><p class="q-eyebrow">Production boundary</p><h2>Display data and analytics data remain separated.</h2><p>TradingView is an external human-readable display surface. Qelly calculations may only consume separately governed provider observations whose rights cover the intended use. Binance and Coinbase remain blocked until that condition is proven.</p><div class="q-external-research-actions"><a class="q-button q-button--secondary" href="https://www.ecb.europa.eu/stats/policy_and_exchange_rates/euro_reference_exchange_rates/html/index.en.html" target="_blank" rel="noopener noreferrer nofollow">ECB reference rates ↗</a><a class="q-button q-button--secondary" href="https://www.tradingview.com/" target="_blank" rel="noopener noreferrer nofollow">TradingView ↗</a><a class="q-button q-button--secondary" href="https://www.forexfactory.com/calendar" target="_blank" rel="noopener noreferrer nofollow">Forex Factory ↗</a></div></div></section>
  </section>`;
}
