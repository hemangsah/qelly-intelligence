const display=(value,formatter)=>value==null?'Unavailable':formatter?formatter(value):String(value);

function evidenceInspector({overview,holdings,performance,risk},escapeHtml){
 const rows=[
  ['PERSISTENCE',overview.persistence||'CLOUD RLS'],
  ['TRUTH STATE',overview.truthState||'UNAVAILABLE'],
  ['VALUATION',overview.valuationTruthState||'UNAVAILABLE'],
  ['MARKET SOURCE',overview.marketEvidence?.provider||'No approved observation'],
  ['OBSERVED AT',overview.marketEvidence?.observedAt||'No market observation supplied'],
  ['COVERAGE',`${holdings.items.length} persisted positions · ${performance.points.length} valuation observations`],
  ['RISK',risk.truthState||'UNAVAILABLE'],
  ['CUSTODY','No custodian or broker connection']
 ];
 return `<aside class="q-v53-evidence-inspector" aria-label="Intelligence Inspector"><header><strong>INTELLIGENCE INSPECTOR</strong><span>Persisted positions are separated from valuation and market evidence.</span></header>${rows.map(([label,value])=>`<div class="q-v53-evidence-row"><small>${label}</small><strong>${escapeHtml(String(value))}</strong></div>`).join('')}<div class="q-v53-evidence-note">Positions are authenticated workspace records under RLS. Qelly does not invent prices, P&amp;L, performance or risk when governed market observations are absent. No order, transfer, custody or wallet action exists.</div></aside>`;
}

export async function renderPortfolioAnalytics(main,deps){
 const {api,pageHead,stateBanner,escapeHtml,QellyDataGrid,formatCompact}=deps;
 const [overview,holdings,performance,risk]=await Promise.all([api('/api/v1/portfolio/overview'),api('/api/v1/portfolio/holdings'),api('/api/v1/portfolio/performance?range=1y'),api('/api/v1/portfolio/risk')]);
 const value=(number)=>display(number,formatCompact);
 main.innerHTML=`<section class="q-page" data-portfolio-persistence="CLOUD RLS" data-portfolio-valuation-truth="UNAVAILABLE">${pageHead('Qelly Intelligence','Portfolio Analytics','Authenticated workspace portfolio records backed by Supabase RLS. Valuation, performance and risk remain unavailable until governed market observations and historical valuation evidence exist.',`<button class="q-button q-button--secondary" data-action="research">Open research workspace</button><button class="q-button q-button--secondary" disabled aria-disabled="true">Broker connections disabled</button>`)}${stateBanner()}
 <div class="q-kpi-grid"><article class="q-kpi"><div class="q-kpi-label">Portfolios</div><div class="q-kpi-value">${overview.portfolioCount}</div><div class="q-kpi-meta"><span>${escapeHtml(overview.name)}</span><span class="q-status q-status--live">cloud RLS</span></div></article><article class="q-kpi"><div class="q-kpi-label">Persisted positions</div><div class="q-kpi-value">${overview.holdingsCount}</div><div class="q-kpi-meta"><span>${escapeHtml(overview.baseCurrency)}</span><span class="q-status q-status--cached">workspace data</span></div></article><article class="q-kpi"><div class="q-kpi-label">Portfolio value</div><div class="q-kpi-value">${value(overview.totalValue)}</div><div class="q-kpi-meta"><span>No approved valuation observation</span><span class="q-status q-status--unavailable">UNAVAILABLE</span></div></article><article class="q-kpi"><div class="q-kpi-label">Performance / risk</div><div class="q-kpi-value">Unavailable</div><div class="q-kpi-meta"><span>Requires governed history</span><span class="q-status q-status--unavailable">UNAVAILABLE</span></div></article></div>
 <div class="q-v53-evidence-workspace"><div class="q-v53-evidence-primary">
  <section class="q-panel"><div class="q-panel-head"><div><h2>Valuation &amp; performance</h2><p>${escapeHtml(performance.methodology)}</p></div><span class="q-status q-status--unavailable">UNAVAILABLE</span></div><div class="q-panel-body"><div class="q-empty-state"><strong>No governed valuation series</strong><p>${escapeHtml(performance.reason)}</p></div></div></section>
  <section class="q-panel"><div class="q-panel-head"><div><h2>Persisted positions</h2><p>User-entered/imported workspace records. They are not reconciled brokerage balances and are not automatically priced.</p></div><span class="q-status q-status--live">CLOUD RLS</span></div><div id="portfolio-grid"></div></section>
  <section class="q-panel"><div class="q-panel-head"><div><h2>Risk evidence</h2><p>${escapeHtml(risk.methodology)}</p></div><span class="q-status q-status--unavailable">UNAVAILABLE</span></div><div class="q-panel-body q-stack"><div class="q-record-row"><span><strong>Annualized volatility</strong><small>Requires return history</small></span><strong>Unavailable</strong></div><div class="q-record-row"><span><strong>95% one-day VaR</strong><small>Requires governed valuation history</small></span><strong>Unavailable</strong></div><div class="q-record-row"><span><strong>Maximum drawdown</strong><small>Requires historical portfolio values</small></span><strong>Unavailable</strong></div><div class="q-record-row"><span><strong>Stress scenarios</strong><small>Not calculated from unpriced positions</small></span><strong>Unavailable</strong></div></div></section>
 </div>${evidenceInspector({overview,holdings,performance,risk},escapeHtml)}</div>
 <section class="q-v53-evidence-activity" aria-label="Portfolio evidence activity"><span>READ ONLY</span><span>CLOUD RLS</span><span>${holdings.items.length} persisted positions</span><span>valuation unavailable</span><span>risk unavailable</span><span>broker and custody unavailable</span></section></section>`;
 new QellyDataGrid(document.getElementById('portfolio-grid'),{caption:'Persisted portfolio positions',columns:[{key:'symbol',label:'Symbol',width:90},{key:'name',label:'Instrument',width:190},{key:'assetClass',label:'Class',width:100},{key:'quantityDisplay',label:'Quantity',width:110},{key:'sourceKind',label:'Source',width:110},{key:'priceDisplay',label:'Price',width:120},{key:'marketValueDisplay',label:'Market value',width:140},{key:'freshnessClass',label:'Market state',width:120,format:'status'}],rows:holdings.items.map(item=>({...item,quantityDisplay:item.quantity==null?'Not supplied':String(item.quantity),priceDisplay:'Unavailable',marketValueDisplay:'Unavailable'}))});
 main.querySelector('[data-action="research"]').addEventListener('click',()=>deps.navigate('research-workspace'));
}
