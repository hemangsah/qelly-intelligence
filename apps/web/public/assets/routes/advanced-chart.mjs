const STYLE_ID='qelly-advanced-chart-v2-style';
function ensureStyles(){if(document.getElementById(STYLE_ID))return;const link=document.createElement('link');link.id=STYLE_ID;link.rel='stylesheet';link.href='./assets/routes/advanced-chart-v2.css?v=20260831-chart2';document.head.append(link);}
const upper=(value)=>String(value||'unavailable').replace(/[-_]+/g,' ').toUpperCase();
const money=(value)=>Number.isFinite(Number(value))?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',notation:Math.abs(Number(value))>=1e6?'compact':'standard',maximumFractionDigits:2}).format(Number(value)):'—';

export async function renderAdvancedChart(main,{api,escapeHtml,toast,navigate,asset}){
  ensureStyles();const safe=(value)=>escapeHtml(String(value??''));let data;
  try{data=await api(`/api/v1/discovery/advanced-chart?asset=${encodeURIComponent(asset||'QI-CRYPTO-BTC')}`);}catch(error){main.innerHTML=`<section class="q-ac-page"><div class="q-ac-error"><p class="q-eyebrow">Qelly Intelligence · Advanced Chart Studio</p><h1>The governed study contract is unavailable.</h1><p>${safe(error.message)} No chart, price history, indicator value or interpretation was substituted.</p><button type="button" data-ac-retry>Retry contract</button></div></section>`;main.querySelector('[data-ac-retry]').addEventListener('click',()=>void renderAdvancedChart(main,{api,escapeHtml,toast,navigate,asset}));main.removeAttribute('aria-busy');return;}
  const selected=data.selected;const study=data.selectedStudy;const config=data.configuration;const series=data.historicalSeries;
  const assets=data.assets.map((item)=>`<option value="${safe(item.id)}" ${item.id===selected.id?'selected':''}>${safe(item.symbol)} · ${safe(item.name)}</option>`).join('');
  const studies=data.studies.map((item)=>`<option value="${safe(item.id)}" ${item.id===study.id?'selected':''}>${safe(item.label)} · ${safe(item.family)}</option>`).join('');
  const intervals=data.intervals.map((item)=>`<option value="${safe(item.id)}" ${item.id===config.interval?'selected':''}>${safe(item.label)}</option>`).join('');
  const ranges=data.ranges.map((item)=>`<option value="${safe(item.id)}" ${item.id===config.range?'selected':''}>${safe(item.label)}</option>`).join('');
  const observation=data.currentObservation;
  main.innerHTML=`<section class="q-ac-page" data-advanced-chart-experience="governed-study-workspace-v2" data-series-state="${safe(series.state)}">
  <header class="q-ac-hero"><div><p class="q-eyebrow">Qelly Intelligence · Advanced Chart Studio</p><h1>Study price structure without inventing the tape.</h1><p>${safe(data.job)}</p><div class="q-ac-hero-actions"><label><span>Instrument</span><select data-ac-asset aria-label="Select study instrument">${assets}</select></label><button type="button" data-ac-route="decision-provenance">Open Decision Provenance</button></div></div><aside><span>Unique job · governed technical study</span><strong>One instrument. One method. One reproducible parameter receipt.</strong><p>Global Charts compares macro relationships. Indicator Library explains formulas. This studio qualifies one price series and applies one bounded study plan.</p></aside></header>
  <section class="q-ac-boundary"><span></span><div><strong>Series unavailable · study planning remains fully available</strong><p>${safe(series.reason)}</p></div><a href="${safe(series.externalDisplay.url)}" target="_blank" rel="noopener noreferrer">Open external display ↗</a></section>
  <section class="q-ac-snapshot"><article><span>Resolved instrument</span><strong>${safe(selected.symbol)}</strong><small>${safe(selected.id)}</small></article><article><span>Latest attributed context</span><strong>${observation?money(observation.priceUsd):'Unavailable'}</strong><small>${observation?safe(`${upper(observation.truthState)} · ${observation.provider}`):'No value substituted'}</small></article><article><span>Series bars</span><strong>${safe(series.pointCount)}</strong><small>${safe(config.targetBars)} targeted · ${safe(config.minimumBars)} minimum</small></article><article><span>Quality gates</span><strong>${safe(data.readiness.readyGates)} / ${safe(data.readiness.totalGates)}</strong><small>${safe(upper(data.readiness.state))}</small></article><article><span>Computed studies</span><strong>${safe(data.readiness.computedStudies)}</strong><small>No inputs · no calculation</small></article></section>
  <section class="q-ac-workbench"><header><div><p class="q-eyebrow">Step 01 · configure the study</p><h2>Make every analytical choice explicit.</h2><p>Controls update the reconstruction receipt locally. The backend owns allowed studies, parameter limits, warm-up and quality requirements.</p></div><button type="button" data-ac-reset>Reset protocol</button></header><div class="q-ac-workbench-grid"><form data-ac-form><label><span>Technical study</span><select name="study" aria-label="Technical study">${studies}</select></label><label><span>Interval</span><select name="interval" aria-label="Study interval">${intervals}</select></label><label><span>Window</span><select name="range" aria-label="Study window">${ranges}</select></label><label><span>Lookback length</span><input name="length" type="number" min="2" max="250" value="${safe(config.length)}" aria-label="Lookback length"></label><label><span>Confirmation closes</span><input name="confirmation" type="number" min="1" max="10" value="${safe(config.confirmation)}" aria-label="Confirmation closes"></label><label><span>Decision horizon</span><select name="horizon" aria-label="Decision horizon"><option>Next review</option><option>24 hours</option><option selected>7 days</option><option>30 days</option></select></label><label class="q-ac-wide"><span>Descriptive hypothesis</span><textarea name="hypothesis" rows="3" maxlength="420" placeholder="Example: Daily closes remain above the configured baseline with a positive slope."></textarea></label><label class="q-ac-wide"><span>Invalidation condition</span><textarea name="invalidation" rows="3" maxlength="420" placeholder="State the observable condition that would overturn the interpretation."></textarea></label><button class="q-ac-run" type="button" data-ac-run>Run readiness check</button></form><div class="q-ac-receipt" data-ac-receipt aria-live="polite"></div></div></section>
  <section class="q-ac-canvas"><header><div><p class="q-eyebrow">Step 02 · qualify the input</p><h2>The empty canvas is an evidence result.</h2><p>A chart appears only after a rights-authorized series passes the gates below. External display values never cross into Qelly calculations.</p></div><span>${safe(upper(series.state))}</span></header><div class="q-ac-canvas-body"><div class="q-ac-axis" aria-label="Historical chart unavailable"><span>GOVERNED SERIES REQUIRED</span><strong>0 bars connected</strong><p>${safe(series.reason)}</p><div><i></i><i></i><i></i><i></i><i></i></div></div><aside><strong>Display / analytics boundary</strong><p>TradingView is available as a separate human-readable display. Qelly does not read, scrape, transform, persist or use its widget values.</p><a href="${safe(series.externalDisplay.url)}" target="_blank" rel="noopener noreferrer">Inspect ${safe(selected.symbol)} externally ↗</a></aside></div></section>
  <section class="q-ac-gates"><header><div><p class="q-eyebrow">Step 03 · pass the series contract</p><h2>Six gates before one calculation.</h2></div><span>${safe(data.readiness.readyGates)} READY · ${safe(data.readiness.totalGates-data.readiness.readyGates)} BLOCKED</span></header><div>${data.qualityGates.map((item,index)=>`<article data-gate-state="${safe(item.state)}"><header><span>${String(index+1).padStart(2,'0')}</span><b>${safe(upper(item.state))}</b></header><h3>${safe(item.label)}</h3><p>${safe(item.purpose)}</p><small>${safe(item.detail)}</small></article>`).join('')}</div></section>
  <section class="q-ac-method"><div><p class="q-eyebrow">Step 04 · understand the method</p><h2 data-ac-method-title>${safe(study.label)}</h2><strong data-ac-method-family>${safe(study.family)}</strong><p data-ac-method-job>${safe(study.job)}</p><dl><div><dt>Calculation</dt><dd data-ac-method-formula>${safe(study.method)}</dd></div><div><dt>Interpretation</dt><dd data-ac-method-interpretation>${safe(study.interpretation)}</dd></div><div><dt>Required warm-up</dt><dd data-ac-method-warmup>${safe(study.warmup)}</dd></div><div><dt>Invalidation discipline</dt><dd data-ac-method-invalidation>${safe(study.invalidation)}</dd></div></dl></div><aside><span>Known failure modes</span><ol data-ac-failures>${study.failureModes.map((item)=>`<li>${safe(item)}</li>`).join('')}</ol><button type="button" data-ac-route="indicator-library">Inspect formula definitions →</button></aside></section>
  <section class="q-ac-protocol"><header><p class="q-eyebrow">Reproducible workflow</p><h2>Five stages from symbol to provenance.</h2></header><div>${data.protocol.map((item)=>`<article><span>${safe(item.step)}</span><h3>${safe(item.label)}</h3><p>${safe(item.job)}</p></article>`).join('')}</div></section>
  <section class="q-ac-handoffs"><header><div><p class="q-eyebrow">Specialist handoffs</p><h2>Continue without duplicating the job.</h2></div><p>The receipt carries method choices; the next workspace adds narrative evidence, data engineering or decision governance.</p></header><div>${data.handoffs.map((item)=>`<button type="button" data-ac-route="${safe(item.route)}"><strong>${safe(item.label)}</strong><span>${safe(item.job)}</span><i>↗</i></button>`).join('')}</div></section>
  <footer class="q-ac-footer"><strong>${safe(data.version)}</strong><span>No fake candles · no external-widget ingestion · no forecast · no recommendation · no execution</span></footer></section>`;
  main.removeAttribute('aria-busy');const form=main.querySelector('[data-ac-form]');const receipt=main.querySelector('[data-ac-receipt]');const catalog=new Map(data.studies.map((item)=>[item.id,item]));
  const selectedMethod=()=>catalog.get(form.elements.study.value)||data.studies[0];
  const renderMethod=()=>{const current=selectedMethod();main.querySelector('[data-ac-method-title]').textContent=current.label;main.querySelector('[data-ac-method-family]').textContent=current.family;main.querySelector('[data-ac-method-job]').textContent=current.job;main.querySelector('[data-ac-method-formula]').textContent=current.method;main.querySelector('[data-ac-method-interpretation]').textContent=current.interpretation;main.querySelector('[data-ac-method-warmup]').textContent=current.warmup;main.querySelector('[data-ac-method-invalidation]').textContent=current.invalidation;main.querySelector('[data-ac-failures]').innerHTML=current.failureModes.map((item)=>`<li>${safe(item)}</li>`).join('');};
  const renderReceipt=()=>{const current=selectedMethod();const length=Math.max(current.minLength,Math.min(current.maxLength,Number.parseInt(form.elements.length.value,10)||current.defaultLength));const confirmation=Math.max(1,Math.min(10,Number.parseInt(form.elements.confirmation.value,10)||2));const hypothesis=form.elements.hypothesis.value.trim();const invalidation=form.elements.invalidation.value.trim();const ready=series.pointCount>=length+1&&data.qualityGates.every((item)=>item.state==='ready');receipt.innerHTML=`<header><div><p class="q-eyebrow">Study reconstruction receipt</p><h3>${safe(selected.symbol)} · ${safe(current.label)}</h3></div><span>${ready?'COMPUTE READY':'SERIES BLOCKED'}</span></header><dl><div><dt>Parameter set</dt><dd>${safe(current.id.toUpperCase())} · length ${safe(length)} · ${safe(form.elements.interval.value)} · ${safe(form.elements.range.value)} · ${safe(confirmation)} confirmation ${confirmation===1?'close':'closes'}</dd></div><div><dt>Analytical job</dt><dd>${safe(current.job)}</dd></div><div><dt>Decision horizon</dt><dd>${safe(form.elements.horizon.value)}</dd></div><div><dt>Hypothesis</dt><dd>${safe(hypothesis||'Not written; interpretation is not decision-ready.')}</dd></div><div><dt>Invalidation</dt><dd>${safe(invalidation||'Required before decision handoff.')}</dd></div><div><dt>Evidence boundary</dt><dd>${safe(series.pointCount)} governed bars · ${safe(data.readiness.readyGates)} of ${safe(data.readiness.totalGates)} quality gates ready</dd></div></dl><section><strong>${ready?'Calculation may proceed':'Calculation correctly withheld'}</strong><p>${ready?'The input contract is complete; inspect outputs and failure modes before interpreting.':safe(series.reason)}</p></section><div><button type="button" data-ac-copy>Copy study brief</button><button type="button" data-ac-provenance>Send to provenance →</button></div>`;receipt.querySelector('[data-ac-copy]').addEventListener('click',async()=>{const text=[`${selected.symbol} · ${current.label}`,`Parameters: length ${length}, ${form.elements.interval.value}, ${form.elements.range.value}, ${confirmation} confirmation closes`,`Hypothesis: ${hypothesis||'Not written'}`,`Invalidation: ${invalidation||'Not written'}`,`Series: ${series.pointCount} governed bars; calculation withheld`].join('\n');try{await navigator.clipboard.writeText(text);toast?.('Study brief copied.');}catch{toast?.('Clipboard access is unavailable.');}});receipt.querySelector('[data-ac-provenance]').addEventListener('click',()=>navigate('decision-provenance',selected.id));};
  renderMethod();renderReceipt();form.addEventListener('input',()=>{renderMethod();renderReceipt();});form.addEventListener('change',()=>{const current=selectedMethod();if(Number(form.elements.length.value)<current.minLength||Number(form.elements.length.value)>current.maxLength)form.elements.length.value=current.defaultLength;renderMethod();renderReceipt();});
  main.querySelector('[data-ac-run]').addEventListener('click',()=>{renderReceipt();receipt.scrollIntoView({behavior:'smooth',block:'nearest'});toast?.('Readiness check complete: governed series is still required.');});
  main.querySelector('[data-ac-reset]').addEventListener('click',()=>{form.reset();form.elements.study.value=data.selectedStudy.id;form.elements.interval.value=config.interval;form.elements.range.value=config.range;form.elements.length.value=config.length;form.elements.confirmation.value=config.confirmation;renderMethod();renderReceipt();toast?.('Study protocol reset.');});
  main.querySelector('[data-ac-asset]').addEventListener('change',(event)=>navigate('advanced-chart',event.target.value));
  main.querySelectorAll('[data-ac-route]').forEach((button)=>button.addEventListener('click',()=>navigate(button.dataset.acRoute,selected.id)));
}
