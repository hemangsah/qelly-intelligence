# Qelly implementation authorization — 2026-08-07

## Authorization recorded

The owner instruction in the main Qelly implementation conversation authorizes continued UI, frontend and backend implementation against the Terminal OS blueprint and the current 70-route repository baseline.

This authorization supersedes the earlier pre-design hold for Qelly-original implementation work. It does **not** convert missing donor-source evidence into an approved licence state.

## Implementation boundary

The implementation may proceed when it:

- preserves Qelly ownership of product logic, information architecture, evidence semantics and identity;
- remains read-only and does not introduce trade execution, custody, transfers, deposits, withdrawals, swaps or secret collection;
- uses Qelly-governed tokens, components and terminology;
- keeps source, freshness, confidence, methodology, assumptions, contradictions, limitations and auditability visible where decision-relevant;
- treats missing evidence as missing rather than inventing production truth;
- preserves the canonical 70-route registry and compatibility aliases;
- adds tests with each shell, component, API or state-contract change.

## Donor-source boundary remains active

Editable donor kits and exact commercial/licence evidence are still absent from the governed source package. Therefore this authorization permits original Qelly implementation informed by the documented role hierarchy, but does not permit copying, publishing or redistributing vendor-derived screens, assets or source files.

## Deployment boundary

This record authorizes implementation on the active review branch. It does not by itself authorize merge, production deployment, Supabase migration application, or a change of canonical hosting/runtime target.
