function securityInspector(status){
 const rows=[
  ['TRUTH STATE',status.enabled?'MFA enabled for current authenticated account':'MFA not enabled for current authenticated account'],
  ['SOURCE','Authenticated /api/v1/auth/mfa/status'],
  ['OBSERVED AT','Current request · server timestamp not supplied'],
  ['CONFIDENCE','Direct authenticated status response'],
  ['METHOD','TOTP enrollment with server-side confirmation'],
  ['RECOVERY','Single-use recovery codes after confirmation'],
  ['BOUNDARY','No private keys, wallet seeds or financial recovery phrases']
 ];
 return `<aside class="q-v53-final-inspector" aria-label="Security evidence Inspector"><header><strong>INTELLIGENCE INSPECTOR</strong><span>Identity assurance state is separated from enrollment actions and recovery material.</span></header>${rows.map(([label,value])=>`<div class="q-v53-final-row"><small>${label}</small><strong>${value}</strong></div>`).join('')}<div class="q-v53-final-note">Authenticator enrollment changes account security only. It does not authorize trading, custody, transfers, wallet signing or money movement.</div></aside>`;
}

export async function renderSecuritySetup(main,{api,pageHead,toast}){
 const status=await api('/api/v1/auth/mfa/status');
 main.innerHTML=`<section class="q-page">${pageHead('Identity assurance','Authenticator & Recovery','Enroll time-based one-time passwords and generate single-use recovery codes. Enrollment secrets are shown only during setup and remain server-side after confirmation.')}<div class="q-kpi-grid"><article class="q-kpi"><div class="q-kpi-label">Authenticator</div><div class="q-kpi-value q-kpi-value--text">${status.enabled?'Enabled':'Not enabled'}</div><div class="q-kpi-meta"><span>current authenticated account</span><span class="q-status q-status--${status.enabled?'live':'warning'}">${status.enabled?'enabled':'setup required'}</span></div></article><article class="q-kpi"><div class="q-kpi-label">TOTP window</div><div class="q-kpi-value">±30s</div><div class="q-kpi-meta"><span>six-digit code</span><span class="q-status q-status--cached">method</span></div></article><article class="q-kpi"><div class="q-kpi-label">Recovery codes</div><div class="q-kpi-value">10</div><div class="q-kpi-meta"><span>issued after confirmation</span><span class="q-status q-status--cached">single use</span></div></article><article class="q-kpi"><div class="q-kpi-label">Financial authority</div><div class="q-kpi-value">None</div><div class="q-kpi-meta"><span>identity assurance only</span><span class="q-status q-status--unavailable">no execution</span></div></article></div><div class="q-v53-final-workspace"><div class="q-v53-final-primary"><section class="q-panel"><div class="q-panel-head"><div><h2>MFA status</h2><p>Authenticated identity-assurance state from the production MFA status endpoint.</p></div><span class="q-status q-status--${status.enabled?'live':'warning'}">${status.enabled?'enabled':'not enabled'}</span></div><div class="q-panel-body q-stack"><button class="q-button q-button--primary" data-enroll ${status.enabled?'disabled aria-disabled="true"':''}>${status.enabled?'Authenticator already enabled':'Begin authenticator enrollment'}</button><div data-enrollment></div></div></section><section class="q-panel"><div class="q-panel-head"><div><h2>Security boundaries</h2><p>Recovery material is narrowly scoped to account authentication.</p></div><span class="q-status q-status--cached">fail closed</span></div><div class="q-panel-body"><ul><li>Six-digit TOTP with ±30 second tolerance.</li><li>Ten one-time recovery codes after confirmation.</li><li>Audited enrollment and disable events.</li><li>Passkeys are managed separately in the Passkey Center.</li><li>Wallet recovery phrases, private keys and signing secrets are never requested.</li></ul></div></section></div>${securityInspector(status)}</div><section class="q-v53-final-activity" aria-label="Security setup evidence activity"><span>IDENTITY ONLY</span><span>${status.enabled?'MFA enabled':'MFA setup required'}</span><span>TOTP confirmation server-side</span><span>recovery codes single-use</span><span>no financial authority</span></section></section>`;
 main.querySelector('[data-enroll]')?.addEventListener('click',async()=>{try{const e=await api('/api/v1/auth/mfa/enroll',{method:'POST',body:'{}'});const box=main.querySelector('[data-enrollment]');box.innerHTML=`<div class="q-stack"><p>Enter this enrollment secret into your authenticator, then confirm with the six-digit code.</p><code class="q-v53-security-secret">${e.secret}</code><small class="q-v53-security-secret">${e.otpauth}</small><form data-confirm><label>Authenticator code<input name="code" inputmode="numeric" pattern="[0-9]{6}" autocomplete="one-time-code" required></label><button class="q-button q-button--primary">Confirm MFA</button></form></div>`;box.querySelector('form').onsubmit=async ev=>{ev.preventDefault();const code=new FormData(ev.currentTarget).get('code');const r=await api('/api/v1/auth/mfa/confirm',{method:'POST',body:JSON.stringify({code})});box.innerHTML=`<h3>Recovery codes</h3><pre class="q-v53-security-secret">${r.recoveryCodes.join('\n')}</pre><p>Store these offline. They are displayed once.</p>`;toast('MFA enabled',{tone:'success'});};}catch(e){toast(e.message,{tone:'danger'});}});
}
