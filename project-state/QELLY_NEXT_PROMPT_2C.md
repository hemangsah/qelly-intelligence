# QELLY NEXT PROMPT 2C — Production Readiness Without Execution

Status: **GENERATED ONLY — DO NOT EXECUTE FROM PROMPT 2B**

## Entry guard

Prompt 2C may begin only after Prompt 2B has one immutable exact head with:

- 151 formulas and 54 indicators reconciled;
- 1,944/1,944 browser records passed;
- 54/54 accessibility checks passed;
- zero browser, theme, performance, console, page, resource, action, overflow, navigation, blank-tail, font, truth-label, unlabelled-control and CLS failures;
- complete saved-calculation lifecycle and shared-state restoration passed;
- fetched-back source manifest and fresh provenance verified;
- 70-route/429-frame repository and Figma handoff reconciliation recorded;
- final static preview and checksum-verified 18-section artifact accepted;
- PR #23 still open, draft, unmerged and undeployed.

## Prompt 2C objective

Produce the largest truthful product-readiness increase by adding **production-grade scoped persistence and read-only provider reliability**, while preserving deterministic-local numerical behavior and prohibiting execution capabilities.

## Dependency-ordered implementation wave

1. Freeze the accepted Prompt 2B numerical and design contracts.
2. Implement production PostgreSQL persistence for saved calculations, revisions, imports and shared snapshots using the existing user/tenant/workspace isolation model.
3. Add migration execution, rollback, backup and restore evidence for migrations 107 and 108.
4. Add idempotency, optimistic concurrency, revision conflict handling and audit events to every saved-calculation mutation.
5. Add encrypted-at-rest storage and retention/deletion controls without storing credentials, wallet material or provider secrets in calculation records.
6. Add a read-only external market-data provider adapter behind existing provider contracts.
7. Add provider health, freshness, failover, rate-limit, schema-drift and stale/fallback truth states.
8. Preserve deterministic-local formulas and indicators as provider-independent calculations; provider observations may supply inputs but must never change formula definitions silently.
9. Add production API contract tests, database isolation tests, provider-failure tests and full browser truth-state coverage.
10. Reconcile repository routes/screens, deterministic Figma generation and static-preview fixtures for any new states.
11. Run all gates on one exact head and generate a checksum-verified Prompt 2C artifact.

## Hard boundaries

Prompt 2C must not enable or simulate:

- trading or autonomous execution;
- broker order placement;
- custody, deposits or withdrawals;
- wallet/private-key/seed handling;
- leverage or credit issuance;
- personalized investment recommendations;
- public deployment without an explicit later authorization.

## Completion decision

Prompt 2C is complete only when production persistence and read-only provider reliability are proven on one exact head, product truth remains explicit under every failure state, and no execution or custody capability exists.
