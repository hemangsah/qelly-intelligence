const STYLE_ID='qelly-asset-intelligence-v2-style';
function ensureStyles(){if(document.getElementById(STYLE_ID))return;const link=document.createElement('link');link.id=STYLE_ID;link.rel='stylesheet';link.href='./assets/routes/asset-intelligence-v2.css?v=20260831-asset2';document.head.append(link);}
const number=(value,digits=2)=>Number.isFinite(Number(value))?new Intl.NumberFormat('en-US',{maximumFractionDigits:digits}).format(Number(value)):'—';
const money=(value)=>Number.isFinite(Number(value))?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',notation:Math.abs(Number(value))>=1e6?'compact':'standard',maximumFractionDigits:2}).format(Number(value)):'—';
const upper=(value)=>String(value||'unavailable').replace(/[-_]+/g,' ').toUpperCase();

export async function renderAssetIntelligence(main,{api,escapeHtml,toast,navigate,asset}){
  ensureStyles();const safe=(value)=>escapeHtml(String(value??''));let data;
  try{data=await api(`/api/v1/discovery/asset-intelligence?asset=${encodeURIComponent(asset||'QI-CRYPTO-BTC')}`);}catch(error){main.innerHTML=`<section class="q-ai-page"><div class="q-ai-error"><p class="q-eyebrow">Qelly Intelligence · Asset Intelligence</p><h1>The instrument briefing contract is unavailable.</h1><p>${safe(error.message)} No profile, observation, evidence or recommendation was substituted.</p><button type="button" data-ai-retry>Retry contract</button></div></section>`;main.querySelector('[data-ai-retry]').addEventListener('click',()=>void renderAssetIntelligence(main,{api,escapeHtml,toast,navigate,asset}));main.removeAttribute('aria-busy');return;}
  const selected=data.selected;const observation=selected.observation;const context=selected.independentContext;
  const options=data.assets.map((item)=>`<option value="${safe(item.id)}" ${item.id===selected.id?'selected':''}>${safe(item.symbol)} · ${safe(item.name)}</option>`).join('');
  const lensOptions=data.lenses.map((item)=>`<option value="${safe(item.id)}">${safe(item.label)} · ${safe(item.outcome)}</option>`).join('');
  const stateTone=(value)=>value==='live'?'live':value==='cached'?'cached':value==='delayed'?'delayed':value==='available'?'live':'unavailable';
  main.innerHTML=`<section class="q-ai-page" data-asset-intelligence-experience="diligence-router-v2" data-asset-state="${safe(data.state)}">
  <header class="q-ai-hero"><div><p class="q-eyebrow">Qelly Intelligence · Asset Intelligence</p><h1>Know what you know. Route what you still need.</h1><p>${safe(data.job)}</p><div><label><span>Instrument</span><select data-ai-asset aria-label="Select instrument">${options}</select></label><a class="q-button q-button--primary" href="#/news-research">Ask Qelly about ${safe(selected.symbol)}</a></div></div><aside><span>Unique job · single-instrument diligence routing</span><strong>Asset Intelligence diagnoses evidence readiness before deep analysis begins.</strong><p>It does not replace a chart, filing reader, event calendar, comparison lab, persistent dossier or human decision.</p></aside></header>
  <section class="q-ai-truth"><span></span><strong>Identity is not a price · context is not confirmation · a brief is not advice</strong><p>${safe(data.summary.readyTracks)} of ${safe(data.summary.evidenceTracks)} evidence tracks currently have usable inputs</p></section>
  <section class="q-ai-snapshot"><article><span>Resolved instrument</span><strong>${safe(selected.symbol)}</strong><small>${safe(selected.id)}</small></article><article><span>Attributed observation</span><strong>${observation?money(observation.priceUsd):'Unavailable'}</strong><small>${observation?safe(`${upper(observation.truthState)} · ${observation.provider}`):'No value substituted'}</small></article><article><span>24h source change</span><strong>${observation?`${number(observation.change24hPct)}%`:'—'}</strong><small>${observation?'Source-reported context; not a forecast':'Observation missing'}</small></article><article><span>Independent context</span><strong>${context?money(context.valueUsd):'Unavailable'}</strong><small>${context?safe(`${context.provider} · not confirmation`):'Second source missing'}</small></article><article><span>Evidence readiness</span><strong>${safe(data.summary.readyTracks)} / ${safe(data.summary.evidenceTracks)}</strong><small>Missing tracks stay visible</small></article></section>
  <section class="q-ai-identity"><div><p class="q-eyebrow">Instrument resolution</p><h2>${safe(selected.name)} <span>${safe(selected.symbol)}</span></h2><p>${safe(selected.role)} on ${safe(selected.network)}. The stable Qelly identifier anchors the brief; it does not assert ownership, suitability or a current valuation.</p></div><dl><div><dt>Canonical ID</dt><dd>${safe(selected.id)}</dd></div><div><dt>Asset class</dt><dd>${safe(selected.assetClass)}</dd></div><div><dt>Network scope</dt><dd>${safe(selected.network)}</dd></div><div><dt>Personalized view</dt><dd>DISABLED</dd></div></dl></section>
  <section class="q-ai-builder"><header><div><p class="q-eyebrow">Step 01 · frame the investigation</p><h2>Build a falsifiable instrument brief.</h2><p>The structure updates locally. It does not save a dossier, generate a recommendation or claim evidence that is not connected.</p></div><button type="button" data-ai-reset>Reset brief</button></header><div class="q-ai-builder-grid"><form data-ai-form><label><span>Research lens</span><select name="lens" aria-label="Research lens">${lensOptions}</select></label><label><span>Review horizon</span><select name="horizon" aria-label="Review horizon"><option>24 hours</option><option>7 days</option><option selected>30 days</option><option>90 days</option><option>12 months</option></select></label><label class="q-ai-wide"><span>Question</span><textarea name="question" rows="3" maxlength="360" aria-label="Research question"></textarea></label><label class="q-ai-wide"><span>Working thesis</span><textarea name="thesis" rows="3" maxlength="360" aria-label="Working thesis" placeholder="What do you currently think, and why?"></textarea></label><label class="q-ai-wide"><span>Invalidation condition</span><textarea name="invalidation" rows="3" maxlength="360" aria-label="Invalidation condition" placeholder="What observable evidence would make the thesis wrong?"></textarea></label></form><div class="q-ai-receipt" data-ai-receipt aria-live="polite"></div></div></section>
  <section class="q-ai-evidence"><header><div><p class="q-eyebrow">Step 02 · inspect evidence readiness</p><h2>Six tracks, with every gap left visible.</h2><p>A missing source is a routing decision—not an invitation to improvise a value.</p></div><span>${safe(data.summary.readyTracks)} usable now</span></header><div>${data.evidenceTracks.map((item,index)=>`<article data-track-state="${safe(item.state)}"><header><span>${String(index+1).padStart(2,'0')}</span><b class="q-ai-state q-ai-state--${stateTone(item.state)}">${safe(upper(item.state))}</b></header><h3>${safe(item.label)}</h3><p>${safe(item.purpose)}</p><small>${safe(item.detail)}</small><button type="button" data-ai-route="${safe(item.nextRoute)}">${safe(item.nextLabel)} →</button></article>`).join('')}</div></section>
  <section class="q-ai-lenses"><header><p class="q-eyebrow">Step 03 · choose the specialist outcome</p><h2>Each lens answers a different question.</h2></header><div>${data.lenses.map((item)=>`<article><span>${safe(item.label)}</span><h3>${safe(item.question)}</h3><p>${safe(item.outcome)}</p><small>Required tracks · ${safe(item.requiredTracks.join(' + '))}</small><button type="button" data-ai-route="${safe(item.nextRoute)}">Open specialist workspace →</button></article>`).join('')}</div></section>
  <section class="q-ai-handoffs"><header><div><p class="q-eyebrow">Diligence route map</p><h2>One briefing, six non-overlapping handoffs.</h2></div><p>Asset Intelligence coordinates the investigation. Specialist workspaces perform the detailed task.</p></header><div>${data.handoffs.map((item)=>`<button type="button" data-ai-route="${safe(item.route)}"><strong>${safe(item.label)}</strong><span>${safe(item.job)}</span><i>↗</i></button>`).join('')}</div></section>
  <section class="q-ai-ledger"><div><p class="q-eyebrow">Source and boundary receipt</p><h2>Reconstruct what entered the brief.</h2><p>The identity catalog remains separate from observations. Source timestamps and unavailable states are preserved without simulated replacements.</p></div><div>${data.sourceLedger.map((item)=>`<article><header><strong>${safe(item.label)}</strong><span class="q-ai-state q-ai-state--${stateTone(item.truthState)}">${safe(upper(item.truthState))}</span></header><p>${safe(item.role)}</p><dl><div><dt>Observed</dt><dd>${safe(item.observedAt||'Not supplied')}</dd></div><div><dt>Usage</dt><dd>${safe(item.usage||'Governed public context')}</dd></div></dl></article>`).join('')||'<article><header><strong>No observation source connected</strong><span class="q-ai-state q-ai-state--unavailable">UNAVAILABLE</span></header><p>The identity brief remains usable, while all market values stay empty.</p></article>'}</div><footer><strong>${safe(data.version)}</strong><span>No persistence · no forecast · no recommendation · no execution</span></footer></section></section>`;
  main.removeAttribute('aria-busy');const form=main.querySelector('[data-ai-form]');const receipt=main.querySelector('[data-ai-receipt]');
  const defaults=()=>{form.elements.lens.value='identity';form.elements.horizon.value='30 days';form.elements.thesis.value='';form.elements.invalidation.value='';};
  const renderReceipt=()=>{const lens=data.lenses.find((item)=>item.id===form.elements.lens.value)||data.lenses[0];if(!form.elements.question.value.trim())form.elements.question.value=lens.question;const trackMap=new Map(data.evidenceTracks.map((item)=>[item.id,item]));const required=lens.requiredTracks.map((id)=>trackMap.get(id)).filter(Boolean);const gaps=required.filter((item)=>item.state==='unavailable');receipt.innerHTML=`<header><div><p class="q-eyebrow">Brief reconstruction receipt</p><h3>${safe(selected.symbol)} · ${safe(lens.label)}</h3></div><span>${gaps.length?'EVIDENCE GAPS':'READY TO INVESTIGATE'}</span></header><dl><div><dt>Question</dt><dd>${safe(form.elements.question.value||lens.question)}</dd></div><div><dt>Outcome</dt><dd>${safe(lens.outcome)}</dd></div><div><dt>Review horizon</dt><dd>${safe(form.elements.horizon.value)}</dd></div><div><dt>Required tracks</dt><dd>${required.map((item)=>`${safe(item.label)} · ${safe(upper(item.state))}`).join('<br>')}</dd></div><div><dt>Working thesis</dt><dd>${safe(form.elements.thesis.value||'Not written yet')}</dd></div><div><dt>Invalidation</dt><dd>${safe(form.elements.invalidation.value||'Required before decision use')}</dd></div></dl><section><strong>${gaps.length?`${gaps.length} missing evidence ${gaps.length===1?'track':'tracks'}`:'Required inputs are available'}</strong><p>${gaps.length?safe(gaps.map((item)=>item.label).join(' · ')):'Availability does not prove the thesis; continue to the specialist workspace.'}</p></section><button type="button" data-ai-next="${safe(lens.nextRoute)}">Continue to ${safe(data.handoffs.find((item)=>item.route===lens.nextRoute)?.label||'specialist workspace')} →</button>`;receipt.querySelector('[data-ai-next]').addEventListener('click',(event)=>navigate(event.currentTarget.dataset.aiNext,selected.id));};
  defaults();renderReceipt();form.addEventListener('change',(event)=>{if(event.target.name==='lens'){const lens=data.lenses.find((item)=>item.id===event.target.value);form.elements.question.value=lens?.question||'';}renderReceipt();});form.addEventListener('input',renderReceipt);
  main.querySelector('[data-ai-reset]').addEventListener('click',()=>{defaults();form.elements.question.value=data.lenses[0].question;renderReceipt();toast?.('Instrument brief reset.');});
  main.querySelector('[data-ai-asset]').addEventListener('change',(event)=>navigate('asset-intelligence',event.target.value));
  main.querySelectorAll('[data-ai-route]').forEach((button)=>button.addEventListener('click',()=>navigate(button.dataset.aiRoute,selected.id)));
}
