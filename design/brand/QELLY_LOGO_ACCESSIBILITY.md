# Qelly Logo Accessibility

Meaningful lockups use the accessible name “Qelly”. Decorative symbol repetitions use empty alternative text and `aria-hidden`. The opening uses a polite status role and never traps focus.

High-contrast and monochrome variants remain available. Reduced-motion preferences are respected. Minimum symbol size is 16 px and minimum horizontal lockup size is 120 px.
