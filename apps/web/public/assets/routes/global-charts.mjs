const STYLE_ID='qelly-global-charts-v2-style';
function ensureStyles(){if(document.getElementById(STYLE_ID))return;const link=document.createElement('link');link.id=STYLE_ID;link.rel='stylesheet';link.href='./assets/routes/global-charts-v2.css?v=20260831-global1';document.head.append(link);}
const upper=(value)=>String(value??'unavailable').replace(/[_-]+/g,' ').toUpperCase();
const sourceCard=(item,safe)=>`<article class="q-gc-source" data-global-source="${safe(item.id)}" data-stage="${safe(item.stage.id)}"><header><div><p>${safe(upper(item.integration))}</p><h3>${safe(item.name)}</h3></div><span>${safe(upper(item.stage.label))}</span></header><strong>${safe(item.role)}</strong><p>${safe(item.note)}</p><dl><div><dt>Observations</dt><dd>${item.capabilities.observationsDisplayed?'REFERENCE ONLY':'NOT DISPLAYED'}</dd></div><div><dt>Correlation</dt><dd>NOT CALCULATED</dd></div><div><dt>Forecast</dt><dd>DISABLED</dd></div></dl><footer><p><b>Next gate</b>${safe(item.nextAction)}</p>${item.officialUrl?`<a href="${safe(item.officialUrl)}" target="_blank" rel="noopener noreferrer nofollow">Open official source ↗</a>`:''}</footer></article>`;

export async function renderGlobalChartsWorkspace(main,{api,escapeHtml,toast}){
  ensureStyles();let data;
  try{data=await api('/api/v1/discovery/global-charts');}catch(error){main.innerHTML=`<section class="q-gc-page"><div class="q-gc-error"><p class="q-eyebrow">Qelly Intelligence · Global Charts</p><h1>Relationship planning is unavailable.</h1><p>${escapeHtml(error.message)} No series, correlation, forecast or chart value was substituted.</p><button type="button" data-global-retry>Retry</button></div></section>`;main.querySelector('[data-global-retry]').addEventListener('click',()=>void renderGlobalChartsWorkspace(main,{api,escapeHtml,toast}));main.removeAttribute('aria-busy');return;}
  const safe=(value)=>escapeHtml(String(value??''));const families=data.seriesFamilies||[];const sources=data.sources||[];
  const familyOptions=families.map((item)=>`<option value="${safe(item.id)}">${safe(item.label)}</option>`).join('');
  main.innerHTML=`<section class="q-gc-page" data-global-charts-experience="relationship-blueprint-v1" data-global-state="${safe(data.state)}">
  <header class="q-gc-hero"><div><p class="q-eyebrow">Qelly Intelligence · Global Charts</p><h1>Design the relationship before drawing the line.</h1><p>${safe(data.job)}</p><div><a class="q-button q-button--secondary" href="#/discovery-hub">Inspect governed coverage</a><a class="q-button q-button--primary" href="#/news-research">Challenge it with Qelly</a></div></div><aside><span>Unique job · cross-market relationship testing</span><strong>Global Charts specifies what two series are allowed to mean together.</strong><p>It does not rank assets, map pool mechanics, retrieve an object or turn co-movement into causality.</p></aside></header>
  <section class="q-gc-truth"><span></span><strong>No synthetic line · no discovered correlation · no forecast</strong><p>${safe(data.summary.total)} governed source paths · ${safe(data.summary.displayReady)} governed reference path · 0 calculated relationships</p></section>
  <section class="q-gc-metrics"><article><span>Source paths</span><strong>${safe(data.summary.total)}</strong><small>official and governed research</small></article><article><span>Series families</span><strong>${families.length}</strong><small>distinct economic questions</small></article><article><span>Method gates</span><strong>${(data.protocol||[]).length}</strong><small>before interpretation</small></article><article><span>Transformations</span><strong>${(data.transforms||[]).length}</strong><small>declared before comparison</small></article><article><span>Calculated</span><strong>0</strong><small>no approved pair supplied</small></article></section>
  <section class="q-gc-builder"><header><div><p class="q-eyebrow">Step 01 · specify the relationship</p><h2>Build a falsifiable chart brief.</h2><p>Choose the economic roles, cadence, transformation and lag before seeing any statistical output.</p></div><button type="button" data-global-clear>Clear brief</button></header><div class="q-gc-builder-grid"><form data-global-form><label><span>Leading series family</span><select name="driver" aria-label="Leading series family">${familyOptions}</select></label><label><span>Responding series family</span><select name="response" aria-label="Responding series family">${familyOptions.replace('value="growth"','value="growth"').replace('value="inflation"','value="inflation" selected')}</select></label><label><span>Transformation</span><select name="transform" aria-label="Transformation">${(data.transforms||[]).map((item)=>`<option value="${safe(item.id)}">${safe(item.label)}</option>`).join('')}</select></label><label><span>Observation horizon</span><select name="horizon" aria-label="Observation horizon">${(data.horizons||[]).map((item)=>`<option>${safe(item)}</option>`).join('')}</select></label><label><span>Declared lead / lag</span><input name="lag" aria-label="Declared lead or lag" value="0 periods" autocomplete="off" maxlength="40"></label><label><span>Hypothesis and invalidation</span><textarea name="hypothesis" aria-label="Hypothesis and invalidation" rows="4" placeholder="What relationship should hold, and what would disprove it?"></textarea></label><p>Inputs structure this visible blueprint only. They do not request data, calculate statistics or persist a claim.</p></form><div class="q-gc-blueprint" data-global-blueprint></div></div></section>
  <section class="q-gc-families"><header><p class="q-eyebrow">Step 02 · choose the economic role</p><h2>Eight families, eight different questions.</h2></header><div>${families.map((item,index)=>`<article><span>${String(index+1).padStart(2,'0')}</span><div><strong>${safe(item.label)}</strong><p>${safe(item.question)}</p><small>${safe(item.examples)}</small></div></article>`).join('')}</div></section>
  <section class="q-gc-protocol"><header><p class="q-eyebrow">Step 03 · clear the method gates</p><h2>A line is not evidence until the comparison survives the protocol.</h2></header><ol>${(data.protocol||[]).map((step,index)=>`<li><span>0${index+1}</span><div><strong>${safe(step.label)}</strong><p>${safe(step.purpose)}</p></div><label><input type="checkbox" data-global-gate> reviewed</label></li>`).join('')}</ol><footer><strong data-global-progress>0 / ${(data.protocol||[]).length} method gates reviewed</strong><p>Review state is temporary and does not certify a series or relationship.</p></footer></section>
  <section class="q-gc-directory"><header><div><p class="q-eyebrow">Step 04 · resolve the source path</p><h2>Source readiness—not a gallery of invented charts.</h2><p>Each path exposes its permission and methodology gate. Inclusion is not approval or endorsement.</p></div><div><label><span>Find source</span><input type="search" data-global-search aria-label="Find source" placeholder="Provider or economic role"></label><label><span>Readiness</span><select data-global-stage aria-label="Readiness"><option value="all">All stages</option><option value="governed-reference">Governed reference</option><option value="public-reference">Public reference</option><option value="delivery-review">Delivery review</option><option value="configuration-required">Configuration required</option><option value="external-research">External research</option></select></label></div></header><div class="q-gc-source-head"><strong data-global-count-copy><b data-global-count>${sources.length}</b> source paths match</strong><button type="button" data-global-reset>Reset filters</button></div><div class="q-gc-sources" data-global-sources></div></section>
  <section class="q-gc-method"><div><p class="q-eyebrow">Method and non-claims</p><h2>Co-movement stays separate from cause.</h2><p>${safe(data.methodology.measurement)}</p></div><dl><div><dt>Synthetic series</dt><dd>OFF</dd></div><div><dt>Correlation claim</dt><dd>NONE</dd></div><div><dt>Causality claim</dt><dd>NONE</dd></div><div><dt>Forecast</dt><dd>DISABLED</dd></div><div><dt>Recommendation</dt><dd>DISABLED</dd></div></dl><footer><strong>${safe(data.methodology.version)}</strong><p>${safe(data.methodology.comparison)}</p></footer></section></section>`;

  main.removeAttribute('aria-busy');let query='';let stage='all';const grid=main.querySelector('[data-global-sources]');
  const renderSources=()=>{const rows=sources.filter((item)=>(stage==='all'||item.stage.id===stage)&&(!query||`${item.name} ${item.role} ${item.integration}`.toLowerCase().includes(query)));main.querySelector('[data-global-count-copy]').innerHTML=`<b data-global-count>${rows.length}</b> source ${rows.length===1?'path matches':'paths match'}`;grid.innerHTML=rows.length?rows.map((item)=>sourceCard(item,safe)).join(''):'<div class="q-gc-empty"><strong>No governed source path matches.</strong><p>Change the source name or readiness stage. No substitute series was created.</p></div>';};
  const form=main.querySelector('[data-global-form]');const renderBlueprint=()=>{const values=Object.fromEntries(new FormData(form).entries());const driver=families.find((item)=>item.id===values.driver);const response=families.find((item)=>item.id===values.response);const transform=(data.transforms||[]).find((item)=>item.id===values.transform);const hypothesis=String(values.hypothesis||'').trim();main.querySelector('[data-global-blueprint]').innerHTML=`<header><div><p class="q-eyebrow">Relationship blueprint</p><h3>${safe(driver?.label)} → ${safe(response?.label)}</h3></div><span>${hypothesis?'FALSIFIABLE BRIEF':'HYPOTHESIS MISSING'}</span></header><div class="q-gc-flow"><article><small>Leading role</small><strong>${safe(driver?.label)}</strong><p>${safe(driver?.question)}</p></article><i aria-hidden="true">→</i><article><small>Transform</small><strong>${safe(transform?.label)}</strong><p>${safe(transform?.use)}</p></article><i aria-hidden="true">→</i><article><small>Responding role</small><strong>${safe(response?.label)}</strong><p>${safe(response?.question)}</p></article></div><dl><div><dt>Horizon</dt><dd>${safe(values.horizon)}</dd></div><div><dt>Declared lag</dt><dd>${safe(values.lag||'Not supplied')}</dd></div><div><dt>Hypothesis / invalidation</dt><dd>${safe(hypothesis||'Not supplied')}</dd></div></dl><footer>No observations loaded · no statistic calculated · no direction recommended</footer>`;};
  renderSources();renderBlueprint();form.addEventListener('input',renderBlueprint);main.querySelector('[data-global-search]').addEventListener('input',(event)=>{query=event.target.value.trim().toLowerCase();renderSources();});main.querySelector('[data-global-stage]').addEventListener('change',(event)=>{stage=event.target.value;renderSources();});main.querySelector('[data-global-reset]').addEventListener('click',()=>{query='';stage='all';main.querySelector('[data-global-search]').value='';main.querySelector('[data-global-stage]').value='all';renderSources();});main.querySelector('[data-global-clear]').addEventListener('click',()=>{form.reset();renderBlueprint();toast?.('Global chart brief cleared.');});main.addEventListener('change',(event)=>{if(!event.target.matches('[data-global-gate]'))return;const checked=main.querySelectorAll('[data-global-gate]:checked').length;main.querySelector('[data-global-progress]').textContent=`${checked} / ${(data.protocol||[]).length} method gates reviewed`;});
}
