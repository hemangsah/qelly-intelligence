export async function renderAuthRecovery(main,{api,toast,navigate}){
  main.innerHTML=`<section class="q-auth-page"><div class="q-auth-hero"><div><p class="q-eyebrow">Account recovery</p><h1>Recover access without weakening security.</h1><p>A single-use challenge is delivered through the configured email adapter, expires after 15 minutes, and revokes all existing sessions after a successful password reset.</p></div><div class="q-auth-proof-grid"><article><strong>15 minutes</strong><span>Challenge expiry</span></article><article><strong>5 attempts</strong><span>Automatic lock</span></article><article><strong>All sessions</strong><span>Revoked after reset</span></article></div></div><div class="q-auth-card"><div><p class="q-eyebrow">Recovery workflow</p><h2>Reset your password</h2><p class="q-muted-copy">Request a code first, then enter the challenge ID, six-digit code, and a new password.</p></div><form class="q-auth-form" data-request><label>Email<input name="email" type="email" autocomplete="email" required></label><button class="q-button q-button--primary">Request recovery code</button><p class="q-form-error" role="alert" data-request-status></p></form><hr><form class="q-auth-form" data-reset><label>Challenge ID<input name="challengeId" required placeholder="rcv_..."></label><label>Six-digit code<input name="code" inputmode="numeric" pattern="[0-9]{6}" autocomplete="one-time-code" required></label><label>New password<input name="newPassword" type="password" minlength="12" autocomplete="new-password" required></label><button class="q-button q-button--secondary">Reset password and revoke sessions</button><p class="q-form-error" role="alert" data-reset-status></p></form><div class="q-auth-footer"><span>Remembered your credentials?</span><button class="q-button q-button--ghost" type="button" data-login>Return to login</button></div></div></section>`;
  main.querySelector('[data-login]').onclick=()=>navigate('auth-login');
  main.querySelector('[data-request]').onsubmit=async event=>{event.preventDefault();const status=main.querySelector('[data-request-status]');status.textContent='';try{const email=new FormData(event.currentTarget).get('email');const result=await api('/api/v1/auth/recovery/request',{method:'POST',body:JSON.stringify({email}),skipCsrf:true});if(result.challengeId)main.querySelector('input[name=challengeId]').value=result.challengeId;if(result.developmentCode)main.querySelector('input[name=code]').value=result.developmentCode;status.textContent=result.challengeId?'Recovery challenge created. Check the configured delivery channel.':'If the account exists, a recovery challenge has been created.';toast('Recovery request accepted',{tone:'success'});}catch(error){status.textContent=error.message;toast(error.message,{tone:'danger'});}};
  main.querySelector('[data-reset]').onsubmit=async event=>{event.preventDefault();const status=main.querySelector('[data-reset-status]');status.textContent='';try{const body=Object.fromEntries(new FormData(event.currentTarget));const result=await api('/api/v1/auth/recovery/reset',{method:'POST',body:JSON.stringify(body),skipCsrf:true});status.textContent=`Password reset complete. ${result.revokedSessions} session(s) revoked.`;toast('Password reset complete',{tone:'success'});setTimeout(()=>navigate('auth-login'),900);}catch(error){status.textContent=error.message;toast(error.message,{tone:'danger'});}};
}
