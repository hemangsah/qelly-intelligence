import {createHash} from 'node:crypto';
import {createServer} from 'node:http';
import {mkdir,open,readFile,readdir,writeFile} from 'node:fs/promises';
import path from 'node:path';
import {chromium} from 'playwright';

const root=process.cwd();
const exactHead=process.env.QELLY_REVIEW_HEAD??process.env.GITHUB_SHA??'LOCAL_UNVERIFIED';
const shardIndex=Number(process.env.QELLY_CAPTURE_SHARD_INDEX??0),shardCount=Number(process.env.QELLY_CAPTURE_SHARD_COUNT??9);
const baselineHead=process.env.QELLY_BASELINE_HEAD??'07598e934eca635d590364ca96d7b96e812c767e';
const currentDist=path.resolve(process.env.QELLY_CURRENT_DIST??'dist/frontend'),baselineDist=path.resolve(process.env.QELLY_BASELINE_DIST??'baseline/dist/frontend');
const output=path.resolve(process.env.QELLY_CAPTURE_OUTPUT??`.qelly-worldclass-capture/shard-${shardIndex}`),pngCurrent=path.join(output,'screens/png'),webpCurrent=path.join(output,'screens/webp'),pngBefore=path.join(output,'before/png'),webpBefore=path.join(output,'before/webp');for(const dir of [output,pngCurrent,webpCurrent,pngBefore,webpBefore])await mkdir(dir,{recursive:true});
const jsonl=path.join(output,'capture.jsonl');await writeFile(jsonl,'');const sha256=b=>createHash('sha256').update(b).digest('hex');
const parse=t=>{const rows=[];let r=[],f='',q=false;for(let i=0;i<t.length;i++){const c=t[i];if(q){if(c==='"'&&t[i+1]==='"'){f+='"';i++;}else if(c==='"')q=false;else f+=c;}else if(c==='"')q=true;else if(c===','){r.push(f);f='';}else if(c==='\n'){r.push(f.replace(/\r$/,''));rows.push(r);r=[];f='';}else f+=c;}if(f||r.length){r.push(f);rows.push(r);}const h=rows.shift()??[];return rows.filter(x=>x.some(Boolean)).map(x=>Object.fromEntries(h.map((k,i)=>[k,x[i]??''])))};
const rows=parse(await readFile(path.join(root,'QELLY_SCREEN_MATRIX.csv'),'utf8')),selected=rows.map((row,index)=>({...row,__index:index})).filter(row=>row.__index%shardCount===shardIndex);
const append=async record=>{const h=await open(jsonl,'a');try{await h.write(`${JSON.stringify(record)}\n`);await h.sync();}finally{await h.close();}};
const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.svg':'image/svg+xml','.png':'image/png','.webp':'image/webp','.ico':'image/x-icon','.webmanifest':'application/manifest+json','.woff2':'font/woff2','.txt':'text/plain; charset=utf-8'};
const serve=(dist,port)=>new Promise(resolve=>{const server=createServer(async(req,res)=>{const u=new URL(req.url,`http://127.0.0.1:${port}`);let rel=decodeURIComponent(u.pathname).replace(/^\/qelly-intelligence\/?/,'');if(!rel||rel.endsWith('/'))rel+='index.html';try{const b=await readFile(path.join(dist,rel));res.writeHead(200,{'content-type':mime[path.extname(rel)]??'application/octet-stream','cache-control':'no-store'});res.end(b);}catch{try{const b=await readFile(path.join(dist,'index.html'));res.writeHead(200,{'content-type':'text/html; charset=utf-8'});res.end(b);}catch{res.writeHead(404);res.end('not found')}}});server.listen(port,'127.0.0.1',()=>resolve(server));});
const currentServer=await serve(currentDist,4180+shardIndex),baselineServer=await serve(baselineDist,4280+shardIndex),currentBase=`http://127.0.0.1:${4180+shardIndex}/qelly-intelligence/`,baselineBase=`http://127.0.0.1:${4280+shardIndex}/qelly-intelligence/`;
const savedSeed={schemaVersion:2,items:[{id:'prompt2b-review-saved',name:'World-class review saved calculation',savedAt:'2026-07-31T00:00:00.000Z',updatedAt:'2026-07-31T00:05:00.000Z',schemaVersion:2,version:2,formulaVersion:'2.0.0',result:{status:'success',formulaId:'fresh-present-value',formulaVersion:'2.0.0',engineVersion:'fresh-formula-engine-2026.1',truthState:'FRESH_REIMPLEMENTATION_2026',outputs:{value:100},evidence:{provenanceStatus:'FRESH_REIMPLEMENTATION_2026'}},notes:'Visual evidence seed',tags:['worldclass','review'],favorite:true,truthState:'DETERMINISTIC LOCAL',revisions:[{revisionId:'worldclass-r1',version:1,createdAt:'2026-07-31T00:00:00.000Z',name:'World-class review saved calculation',result:{status:'success',formulaId:'fresh-present-value',outputs:{value:100}},notes:'Baseline',tags:['worldclass'],favorite:false},{revisionId:'worldclass-r2',version:2,createdAt:'2026-07-31T00:05:00.000Z',name:'World-class review saved calculation',result:{status:'success',formulaId:'fresh-present-value',outputs:{value:100}},notes:'Visual evidence seed',tags:['worldclass','review'],favorite:true}]}]};
const fallback={'formula-detail':'formula-detail/fresh-present-value','indicator-detail':'indicator-detail/fresh-price-momentum','calculator-detail':'calculator-detail/fresh-present-value','saved-calculation-detail':'saved-calculation-detail/prompt2b-review-saved','category-detail':'category-detail/defi','venue-detail':'venue-detail/binance','research-article':'research-article/qelly-risk','asset':'asset/BTC'};
const viewport=v=>{const t=String(v||'').toLowerCase(),m=t.match(/(\d{3,4})\D+(\d{3,4})/);if(m)return {width:+m[1],height:+m[2]};if(t.includes('mobile'))return {width:390,height:844};if(t.includes('tablet'))return {width:768,height:1024};if(t.includes('wide'))return {width:1728,height:1080};return {width:1440,height:1000}};
const theme=v=>{const t=String(v||'').toLowerCase();if(t.includes('porcelain')||t.includes('investor'))return 'porcelain-burgundy';if(t.includes('oled')||t.includes('aggressive'))return 'burgundy-night';if(t.includes('contrast')||t.includes('access'))return 'high-contrast';if(t.includes('quant'))return 'graphite-terminal';if(t.includes('research'))return 'midnight-research';return 'burgundy-command'};
const state=v=>{const t=String(v||'default').toLowerCase();for(const s of ['loading','empty','partial','error','offline','stale','delayed','simulated'])if(t.includes(s))return s;return 'default'};
const hashRoute=row=>row.route==='design-system'?`theme-personas?designFrame=${encodeURIComponent(row.frame_id)}`:fallback[row.route]??row.route??'feature-universe';
const ready=async page=>{await page.waitForSelector('html[data-app-ready="true"]',{timeout:30000});await page.waitForFunction(()=>{const m=document.querySelector('#main');return m&&m.getAttribute('aria-busy')==='false'&&m.childElementCount>0&&(m.textContent??'').trim().length>40},null,{timeout:30000});await page.evaluate(async()=>{await document.fonts?.ready});await page.evaluate(()=>new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r))))};
const apply=async(page,row)=>{const persona=theme(row.persona),preview=state(row.state);await page.evaluate(({persona,preview})=>{const t=document.querySelector('#global-theme-selector');if(t){t.value=persona;t.dispatchEvent(new Event('change',{bubbles:true}))}const s=document.querySelector('#state-selector');if(s&&[...s.options].some(x=>x.value===preview)){s.value=preview;s.dispatchEvent(new Event('change',{bubbles:true}))}document.documentElement.dataset.captureState=preview},{persona,preview});await page.waitForFunction(p=>document.documentElement.dataset.theme===p||document.documentElement.dataset.persona===p,persona,{timeout:10000}).catch(()=>{});if(row.route==='design-system')await page.evaluate(data=>{const m=document.querySelector('#main');if(!m)return;m.innerHTML=`<article class="q-page"><section class="q-worldclass-context"><div class="q-worldclass-context__main"><nav class="q-worldclass-breadcrumb"><a href="#/feature-universe">Qelly</a><span>/</span><a href="#/theme-personas">Design system</a><span>/</span><span>${data.frame_id}</span></nav><p class="q-worldclass-purpose">${data.purpose}</p><div class="q-worldclass-truth"><span class="q-worldclass-truth-chip" data-tone="deterministic">Governed foundation</span><span class="q-worldclass-truth-chip" data-tone="user">${data.viewport}</span></div></div></section><header class="q-page-head"><div><p class="q-eyebrow">${data.page}</p><h1>${data.frame_name}</h1><p>${data.purpose}</p></div></header><section class="q-dashboard-grid"><article class="q-panel"><div class="q-panel-head"><h2>Source and truth</h2></div><div class="q-panel-body"><p>${data.source_requirements}</p><div class="q-state-banner"><strong>${data.state}</strong><p>Text, icon and theme-safe colour encode state.</p></div></div></article><article class="q-panel"><div class="q-panel-head"><h2>Interaction and accessibility</h2></div><div class="q-panel-body"><p><b>Interaction:</b> ${data.interaction_notes}</p><p><b>Accessibility:</b> ${data.accessibility_notes}</p><p><b>Responsive:</b> ${data.responsive_notes}</p><p><b>Backend:</b> ${data.backend_dependencies}</p></div></article></section></article>`},row)};
const toWebp=async(page,png)=>Buffer.from(await page.evaluate(async b64=>{const i=new Image();i.src=`data:image/png;base64,${b64}`;await i.decode();const c=document.createElement('canvas');c.width=i.naturalWidth;c.height=i.naturalHeight;c.getContext('2d').drawImage(i,0,0);return new Promise((resolve,reject)=>c.toBlob(async b=>b?resolve([...new Uint8Array(await b.arrayBuffer())]):reject(new Error('webp failed')),'image/webp',.82))},png.toString('base64')));
const capture=async(browser,base,row,pngPath,webpPath,label)=>{const vp=viewport(row.viewport),context=await browser.newContext({viewport:vp,reducedMotion:String(row.state).toLowerCase().includes('reduced')?'reduce':'no-preference',colorScheme:theme(row.persona)==='porcelain-burgundy'?'light':'dark'});await context.addInitScript(seed=>{sessionStorage.setItem('qelly.brand.opening.v1','seen');localStorage.setItem('qelly.calculations.v1',JSON.stringify(seed))},savedSeed);const page=await context.newPage(),consoleErrors=[],pageErrors=[],failedResources=[];page.on('console',m=>{if(m.type()==='error')consoleErrors.push(m.text())});page.on('pageerror',e=>pageErrors.push(e.message));page.on('requestfailed',r=>{if(r.url().startsWith('http://127.0.0.1'))failedResources.push(r.url())});let metrics={};try{await page.goto(`${base}#/${hashRoute(row)}`,{waitUntil:'domcontentloaded',timeout:30000});await ready(page);await apply(page,row);await page.evaluate(()=>new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r))));metrics=await page.evaluate(()=>{const root=document.documentElement,main=document.querySelector('#main');return {appReady:root.dataset.appReady,worldclassReady:root.dataset.worldclassReady??'baseline',textLength:(main?.textContent??'').trim().length,overflowX:Math.max(0,root.scrollWidth-root.clientWidth),fontStatus:document.fonts.status,unlabeledControls:[...document.querySelectorAll('button,input,select,textarea,a[href]')].filter(e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);if(!r.width||!r.height||s.display==='none'||s.visibility==='hidden'||e.disabled)return false;if(e.matches('button,a'))return !(e.textContent??'').trim()&&!e.getAttribute('aria-label')&&!e.getAttribute('title');const id=e.id;return !e.getAttribute('aria-label')&&!e.getAttribute('aria-labelledby')&&!(id&&document.querySelector(`label[for="${CSS.escape(id)}"]`))&&!e.closest('label')}).length}});const png=await page.screenshot({fullPage:true,type:'png',animations:'disabled'}),webp=await toWebp(page,png);await writeFile(pngPath,png);await writeFile(webpPath,webp);return {status:'passed',label,viewport:vp,metrics,consoleErrors,pageErrors,failedResources,png:{bytes:png.length,sha256:sha256(png)},webp:{bytes:webp.length,sha256:sha256(webp)}}}catch(error){return {status:'failed',label,viewport:vp,error:{name:error.name,message:error.message,stack:error.stack},metrics,consoleErrors,pageErrors,failedResources}}finally{await page.close();await context.close()}};
const browser=await chromium.launch({headless:true}),records=[];try{for(const row of selected){const id=row.frame_id||`QF-${String(row.__index+1).padStart(4,'0')}`,paths={currentPng:path.join(pngCurrent,`${id}.png`),currentWebp:path.join(webpCurrent,`${id}.webp`),beforePng:path.join(pngBefore,`${id}.png`),beforeWebp:path.join(webpBefore,`${id}.webp`)};const current=await capture(browser,currentBase,row,paths.currentPng,paths.currentWebp,'after'),before=await capture(browser,baselineBase,row,paths.beforePng,paths.beforeWebp,'before'),record={schemaVersion:1,exactHead,baselineHead,shardIndex,shardCount,sequence:row.__index+1,frame_id:id,route:row.route,frame_name:row.frame_name,viewport:row.viewport,persona:row.persona,state:row.state,current,before,capture_status:current.status==='passed'&&before.status==='passed'?'passed':'failed',paths:{current_png:path.relative(output,paths.currentPng),current_webp:path.relative(output,paths.currentWebp),before_png:path.relative(output,paths.beforePng),before_webp:path.relative(output,paths.beforeWebp)}};records.push(record);await append(record)}}finally{await browser.close();await new Promise(r=>currentServer.close(r));await new Promise(r=>baselineServer.close(r))}
const summary={schemaVersion:1,exactHead,baselineHead,shardIndex,shardCount,expected:selected.length,records:records.length,passed:records.filter(x=>x.capture_status==='passed').length,failed:records.filter(x=>x.capture_status==='failed').length,missing:Math.max(0,selected.length-records.length),generatedAt:new Date().toISOString()};await writeFile(path.join(output,'SUMMARY.json'),JSON.stringify(summary,null,2)+'\n');const files=[];const walk=async d=>{for(const e of await readdir(d,{withFileTypes:true})){const f=path.join(d,e.name);if(e.isDirectory())await walk(f);else if(!f.endsWith('CHECKSUMS.json')){const b=await readFile(f);files.push({path:path.relative(output,f),bytes:b.length,sha256:sha256(b)})}}};await walk(output);await writeFile(path.join(output,'CHECKSUMS.json'),JSON.stringify({schemaVersion:1,exactHead,shardIndex,fileCount:files.length,files:files.sort((a,b)=>a.path.localeCompare(b.path))},null,2)+'\n');console.log(JSON.stringify(summary,null,2));if(summary.failed||summary.missing)process.exit(1);
