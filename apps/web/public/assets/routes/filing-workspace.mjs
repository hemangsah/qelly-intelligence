const assets=[['QI-EQUITY-AAPL','AAPL'],['QI-EQUITY-NVDA','NVDA']];
export async function renderFilingWorkspace(main,deps){const {api,pageHead,stateBanner,escapeHtml,asset,navigate}=deps;const selected=assets.some(([id,symbol])=>id===asset||symbol===asset)?assets.find(([id,symbol])=>id===asset||symbol===asset)[0]:'QI-EQUITY-AAPL';const listing=await api(`/api/v1/asset-intelligence/${selected}/filings`);const chosen=listing.items[0];const filing=chosen?await api(`/api/v1/asset-intelligence/${selected}/filings/${chosen.filingId}`):null;const selector=`<select id="filing-asset" class="q-inline-select" aria-label="Select filing issuer">${assets.map(([id,symbol])=>`<option value="${id}" ${id===selected?'selected':''}>${symbol}</option>`).join('')}</select>`;main.innerHTML=`<section class="q-page">${pageHead('Qelly Intelligence','Filing Workspace','Searchable deterministic filing sections with stable section citations, linked periods and explicit original-document boundaries.',`${selector}<button class="q-button q-button--secondary" data-action="events">Open event calendar</button><button class="q-button q-button--primary" data-action="fundamentals">Open statements</button>`)}${stateBanner()}<div class="q-kpi-grid"><article class="q-kpi"><div class="q-kpi-label">Filing references</div><div class="q-kpi-value">${listing.items.length}</div><div class="q-kpi-meta"><span>${escapeHtml(selected)}</span><span class="q-status q-status--simulated">fixture</span></div></article><article class="q-kpi"><div class="q-kpi-label">Sections</div><div class="q-kpi-value">${filing?.sections.length??0}</div><div class="q-kpi-meta"><span>citation-addressable</span><span class="q-status q-status--cached">searchable</span></div></article><article class="q-kpi"><div class="q-kpi-label">Original document</div><div class="q-kpi-value">No</div><div class="q-kpi-meta"><span>not packaged</span><span class="q-status q-status--unavailable">unavailable</span></div></article><article class="q-kpi"><div class="q-kpi-label">Live ingestion</div><div class="q-kpi-value">Off</div><div class="q-kpi-meta"><span>regulatory adapter required</span><span class="q-status q-status--unavailable">disabled</span></div></article></div>${filing?`<div class="q-filing-layout"><aside class="q-panel"><div class="q-panel-head"><div><h2>Filing index</h2><p>${escapeHtml(filing.form)} · ${escapeHtml(filing.filedAt)}</p></div></div><div class="q-panel-body q-stack"><input id="filing-search" class="q-search-input" type="search" placeholder="Search filing sections" aria-label="Search filing sections">${filing.sections.map(section=>`<button class="q-section-link" data-section="${escapeHtml(section.sectionId)}"><strong>${escapeHtml(section.title)}</strong><small>${escapeHtml(section.citation.locator)}</small></button>`).join('')}</div></aside><section class="q-panel"><div class="q-panel-head"><div><h2>${escapeHtml(filing.title)}</h2><p>Period ended ${escapeHtml(filing.periodEnd)}</p></div><span class="q-status q-status--simulated">fixture content</span></div><div class="q-panel-body q-filing-document">${filing.sections.map(section=>`<article id="filing-${escapeHtml(section.sectionId)}" data-filing-text="${escapeHtml(`${section.title} ${section.content}`.toLowerCase())}"><p class="q-eyebrow">${escapeHtml(section.citation.citationId)}</p><h3>${escapeHtml(section.title)}</h3><p>${escapeHtml(section.content)}</p><footer>${escapeHtml(section.citation.source)} · ${escapeHtml(section.citation.locator)}</footer></article>`).join('')}</div></section></div>`:'<div class="q-empty-state"><div><h2>No filing fixtures</h2><p>This asset class requires a specialized regulatory source.</p></div></div>'}</section>`;main.querySelector('#filing-asset').addEventListener('change',event=>navigate('filing-workspace',event.target.value));main.querySelector('[data-action="events"]').addEventListener('click',()=>navigate('event-calendar',selected));main.querySelector('[data-action="fundamentals"]').addEventListener('click',()=>navigate('fundamentals-estimates',selected));main.querySelectorAll('[data-section]').forEach(button=>button.addEventListener('click',()=>document.getElementById(`filing-${button.dataset.section}`)?.scrollIntoView({behavior:'smooth',block:'start'})));main.querySelector('#filing-search')?.addEventListener('input',event=>{const query=event.target.value.trim().toLowerCase();main.querySelectorAll('[data-filing-text]').forEach(section=>section.hidden=Boolean(query&&!section.dataset.filingText.includes(query)));});}
