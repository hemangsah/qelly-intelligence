export async function renderSecretRotation(main,{api,pageHead,escapeHtml,toast,renderRoute}){
  const data=await api('/api/v1/security/secret-protection/status');
  const p=data.protector??{};
  const distribution=Object.entries(data.envelopeDistribution??{});
  main.innerHTML=`<section class="q-page">${pageHead('Key assurance','Secret Rotation','Inspect versioned AES-256-GCM envelopes and rewrap protected MFA secrets to the active server-configured key without exposing key material to the browser.')}<div class="q-kpi-grid"><article class="q-kpi"><div class="q-kpi-label">Active key</div><div class="q-kpi-value q-kpi-value--text">${escapeHtml(p.activeKeyId??'legacy')}</div><div class="q-kpi-meta"><span>Server-side identifier</span><span class="q-status q-status--${p.rotationSupported?'live':'warning'}">${p.rotationSupported?'rotation ready':'legacy mode'}</span></div></article><article class="q-kpi"><div class="q-kpi-label">Configured keys</div><div class="q-kpi-value">${Number(p.keyCount??0)}</div><div class="q-kpi-meta"><span>No key material returned</span><span class="q-status q-status--cached">protected</span></div></article><article class="q-kpi"><div class="q-kpi-label">Protected records</div><div class="q-kpi-value">${data.protectedRecordCount}</div><div class="q-kpi-meta"><span>MFA factor secrets</span><span class="q-status q-status--cached">audited</span></div></article><article class="q-kpi"><div class="q-kpi-label">Rewrap state</div><div class="q-kpi-value q-kpi-value--text">${data.rewrapRequired?'required':'current'}</div><div class="q-kpi-meta"><span>Envelope distribution</span><span class="q-status q-status--${data.rewrapRequired?'warning':'live'}">${data.rewrapRequired?'action':'verified'}</span></div></article></div><div class="q-two-column"><section class="q-panel"><div class="q-panel-head"><div><h2>Envelope distribution</h2><p>Counts reveal key versions, never secret values.</p></div></div><div class="q-panel-body q-stack">${distribution.length?distribution.map(([key,count])=>`<div class="q-record-row"><span><strong>${escapeHtml(key)}</strong><small>${key===p.activeKeyId?'Active write key':'Legacy or previous key'}</small></span><span class="q-status q-status--${key===p.activeKeyId?'live':'warning'}">${count}</span></div>`).join(''):'<p class="q-muted-copy">No protected MFA records exist yet.</p>'}</div></section><section class="q-panel"><div class="q-panel-head"><div><h2>Governed rotation</h2><p>Rewrap uses only keys already configured in the server keyring.</p></div></div><div class="q-panel-body"><p>${escapeHtml(data.truthBoundary)}</p><ul><li>Random 96-bit IV per envelope.</li><li>Authenticated purpose-bound additional data.</li><li>Legacy envelopes remain readable only while their key is retained.</li><li>Audit event records counts, active key ID, and failures.</li></ul><button class="q-button q-button--primary" data-rewrap ${p.rotationSupported?'':'disabled'}>Rewrap to active key</button></div></section></div></section>`;
  main.querySelector('[data-rewrap]')?.addEventListener('click',async()=>{try{const result=await api('/api/v1/security/secret-protection/rewrap',{method:'POST',body:'{}'});toast(`Rewrapped ${result.rotated} protected record(s)`,{tone:'success'});await renderRoute();}catch(error){toast(error.message,{tone:'danger'});}});
}
