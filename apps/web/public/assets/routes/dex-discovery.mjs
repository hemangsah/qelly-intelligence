const STYLE_ID='qelly-dex-v2-style';
function ensureStyles(){if(document.getElementById(STYLE_ID))return;const link=document.createElement('link');link.id=STYLE_ID;link.rel='stylesheet';link.href='./assets/routes/dex-discovery-v2.css?v=20260831-dex1';document.head.append(link);}
const upper=(value)=>String(value??'unavailable').replace(/[_-]+/g,' ').toUpperCase();
const sourceCard=(item,safe)=>`<article class="q-dx-source" data-dex-source="${safe(item.id)}" data-stage="${safe(item.stage.id)}"><header><div><p>${safe(upper(item.integration))}</p><h3>${safe(item.name)}</h3></div><span class="q-status q-status--unavailable">${safe(upper(item.stage.label))}</span></header><strong>${safe(item.role)}</strong><p>${safe(item.note)}</p><dl><div><dt>Customer display</dt><dd>BLOCKED</dd></div><div><dt>Wallet access</dt><dd>DISABLED</dd></div><div><dt>Execution</dt><dd>DISABLED</dd></div></dl><footer><p><b>Next gate</b>${safe(item.nextAction)}</p>${item.officialUrl?`<a href="${safe(item.officialUrl)}" target="_blank" rel="noopener noreferrer nofollow">Open official source ↗</a>`:''}</footer></article>`;

const briefMarkup=(values,fields,safe)=>{
  const supplied=[values.chain,values.protocol,values.market,values.hypothesis].filter(Boolean).length;
  return `<div class="q-dx-brief-head"><div><p class="q-eyebrow">Research brief · ${supplied}/4 context fields</p><h3>${safe(values.protocol||'Untitled DEX investigation')}</h3></div><span>${supplied===4?'READY TO COLLECT':'CONTEXT INCOMPLETE'}</span></div><dl><div><dt>Chain / network</dt><dd>${safe(values.chain||'Not supplied')}</dd></div><div><dt>Protocol</dt><dd>${safe(values.protocol||'Not supplied')}</dd></div><div><dt>Pool / market / contract</dt><dd>${safe(values.market||'Not supplied')}</dd></div><div><dt>Question or hypothesis</dt><dd>${safe(values.hypothesis||'Not supplied')}</dd></div></dl><section><strong>Required evidence package</strong><ol>${fields.map((field,index)=>`<li><span>${String(index+1).padStart(2,'0')}</span><label><input type="checkbox" data-dex-evidence><b>${safe(field)}</b></label></li>`).join('')}</ol></section><footer><span data-dex-progress>0 / ${fields.length} evidence items checked</span><p>Checkboxes are a temporary review aid. They do not claim that evidence exists or persist a research result.</p></footer>`;
};

export async function renderDexDiscovery(main,{api,escapeHtml,toast}){
  ensureStyles();let data;
  try{data=await api('/api/v1/discovery/dex');}catch(error){main.innerHTML=`<section class="q-dx-page"><div class="q-dx-error"><p class="q-eyebrow">Qelly Intelligence · DEX Discovery</p><h1>On-chain research planning is unavailable.</h1><p>${escapeHtml(error.message)} No TVL, volume, APR, liquidity or safety value was substituted.</p><button type="button" data-dex-retry>Retry</button></div></section>`;main.querySelector('[data-dex-retry]').addEventListener('click',()=>void renderDexDiscovery(main,{api,escapeHtml,toast}));main.removeAttribute('aria-busy');return;}
  const safe=(value)=>escapeHtml(String(value??''));const sources=Array.isArray(data.sources)?data.sources:[];const fields=data.evidenceFields||[];
  main.innerHTML=`<section class="q-dx-page" data-dex-experience="on-chain-evidence-planner-v1" data-dex-state="${safe(data.state)}"><header class="q-dx-hero"><div><p class="q-eyebrow">Qelly Intelligence · DEX Discovery</p><h1>Interrogate the pool before you trust the yield.</h1><p>${safe(data.job)}</p><div><a class="q-button q-button--secondary" href="#/venues">Compare venue boundaries</a><a class="q-button q-button--primary" href="#/news-research">Research with Qelly</a></div></div><aside><span>Unique job · on-chain evidence planning</span><strong>DEX Discovery maps mechanisms and dependencies.</strong><p>Venues explains where access begins. DEX Discovery frames contract, pool, oracle, bridge, governance and transaction questions before any market claim.</p></aside></header>
  <section class="q-dx-truth"><span></span><strong>No wallet · no transaction · no fabricated metrics</strong><p>${safe(data.summary.total)} governed research sources · 0 display-enabled feeds · TVL, volume, APR and safety unassessed</p></section>
  <section class="q-dx-metrics"><article><span>Research sources</span><strong>${safe(data.summary.total)}</strong><small>governed directory</small></article><article><span>Rights review</span><strong>${safe(data.summary.byStage['rights-review']||0)}</strong><small>terms approval required</small></article><article><span>Configuration</span><strong>${safe(data.summary.byStage['configuration-required']||0)}</strong><small>server-side key required</small></article><article><span>External research</span><strong>${safe(data.summary.byStage['external-research']||0)}</strong><small>manual citation boundary</small></article><article><span>Display feeds</span><strong>0</strong><small>no measurement claims</small></article><article><span>Executable</span><strong>0</strong><small>research-only product</small></article></section>
  <section class="q-dx-builder"><header><div><p class="q-eyebrow">Step 01 · define the on-chain question</p><h2>Build a research case—not a wallet session.</h2><p>Supply only public identifiers and a research hypothesis. Never enter seed phrases, private keys, API secrets or personal wallet credentials.</p></div><button type="button" data-dex-clear>Clear brief</button></header><div class="q-dx-builder-grid"><form data-dex-form><label><span>Chain / network</span><input name="chain" autocomplete="off" placeholder="e.g. Ethereum"></label><label><span>Protocol</span><input name="protocol" autocomplete="off" placeholder="Protocol name"></label><label><span>Pool, market or contract</span><input name="market" autocomplete="off" placeholder="Public identifier or address"></label><label><span>Question / hypothesis</span><textarea name="hypothesis" rows="4" placeholder="What must be verified or falsified?"></textarea></label><p>Inputs stay in this browser view and are used only to structure the visible brief.</p></form><div class="q-dx-brief" data-dex-brief></div></div></section>
  <section class="q-dx-workflow"><header><p class="q-eyebrow">Step 02 · follow the evidence sequence</p><h2>Six gates from identity to decision boundary.</h2></header><ol>${(data.workflow||[]).map((step,index)=>`<li><span>0${index+1}</span><div><strong>${safe(step.label)}</strong><p>${safe(step.purpose)}</p></div></li>`).join('')}</ol></section>
  <section class="q-dx-directory"><header><div><p class="q-eyebrow">Step 03 · choose a governed research source</p><h2>Provider readiness—not protocol ranking.</h2><p>Every source has a different permission or configuration gate. Qelly does not scrape external dashboards or convert directory inclusion into endorsement.</p></div><div><label><span>Find source</span><input type="search" data-dex-search placeholder="Provider or research role"></label><label><span>Integration gate</span><select data-dex-stage><option value="all">All gates</option><option value="rights-review">Rights review</option><option value="configuration-required">Configuration required</option><option value="contract-required">Contract required</option><option value="external-research">External research</option></select></label></div></header><div class="q-dx-source-head"><strong><b data-dex-count>${sources.length}</b> sources match</strong><button type="button" data-dex-reset>Reset filters</button></div><div class="q-dx-sources" data-dex-sources></div></section>
  <section class="q-dx-method"><div><p class="q-eyebrow">Method and non-claims</p><h2>Missing on-chain evidence stays visible.</h2><p>${safe(data.methodology.measurement)}</p></div><dl><div><dt>Ranking</dt><dd>NONE</dd></div><div><dt>TVL / volume / APR</dt><dd>UNASSESSED</dd></div><div><dt>Wallet connection</dt><dd>DISABLED</dd></div><div><dt>Transaction submission</dt><dd>DISABLED</dd></div><div><dt>Execution / custody</dt><dd>DISABLED</dd></div><div><dt>Fabricated fallback</dt><dd>OFF</dd></div></dl><footer><strong>${safe(data.methodology.version)}</strong><p>${safe(data.methodology.comparison)}</p></footer></section></section>`;
  main.removeAttribute('aria-busy');let query='';let stage='all';const grid=main.querySelector('[data-dex-sources]');
  const renderSources=()=>{const rows=sources.filter((item)=>(stage==='all'||item.stage.id===stage)&&(!query||`${item.name} ${item.role} ${item.integration}`.toLowerCase().includes(query)));main.querySelector('[data-dex-count]').textContent=String(rows.length);grid.innerHTML=rows.length?rows.map((item)=>sourceCard(item,safe)).join(''):'<div class="q-dx-empty"><strong>No governed source matches.</strong><p>Change the name or integration gate; no substitute source was invented.</p></div>';};
  const values=()=>Object.fromEntries(new FormData(main.querySelector('[data-dex-form]')).entries());const renderBrief=()=>{main.querySelector('[data-dex-brief]').innerHTML=briefMarkup(values(),fields,safe);};renderSources();renderBrief();
  main.querySelector('[data-dex-form]').addEventListener('input',renderBrief);main.querySelector('[data-dex-search]').addEventListener('input',(event)=>{query=event.target.value.trim().toLowerCase();renderSources();});main.querySelector('[data-dex-stage]').addEventListener('change',(event)=>{stage=event.target.value;renderSources();});
  main.querySelector('[data-dex-reset]').addEventListener('click',()=>{query='';stage='all';main.querySelector('[data-dex-search]').value='';main.querySelector('[data-dex-stage]').value='all';renderSources();});main.querySelector('[data-dex-clear]').addEventListener('click',()=>{main.querySelector('[data-dex-form]').reset();renderBrief();toast?.('DEX research brief cleared.');});
  main.addEventListener('change',(event)=>{if(!event.target.matches('[data-dex-evidence]'))return;const checked=main.querySelectorAll('[data-dex-evidence]:checked').length;main.querySelector('[data-dex-progress]').textContent=`${checked} / ${fields.length} evidence items checked`;});
}
